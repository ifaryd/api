import {
  __commonJS
} from "./chunk-FAW2VN4A.js";

// node_modules/@coreui/coreui/dist/js/coreui.bundle.min.js
var require_coreui_bundle_min = __commonJS({
  "node_modules/@coreui/coreui/dist/js/coreui.bundle.min.js"(exports, module) {
    !function(t, e) {
      "object" == typeof exports && "undefined" != typeof module ? module.exports = e() : "function" == typeof define && define.amd ? define(e) : (t = "undefined" != typeof globalThis ? globalThis : t || self).coreui = e();
    }(exports, function() {
      "use strict";
      const t = "transitionend", e = (t2) => (t2 && window.CSS && window.CSS.escape && (t2 = t2.replace(/#([^\s"#']+)/g, (t3, e2) => `#${CSS.escape(e2)}`)), t2), i = (e2) => {
        e2.dispatchEvent(new Event(t));
      }, n = (t2) => !(!t2 || "object" != typeof t2) && (void 0 !== t2.jquery && (t2 = t2[0]), void 0 !== t2.nodeType), s = (t2) => n(t2) ? t2.jquery ? t2[0] : t2 : "string" == typeof t2 && t2.length > 0 ? document.querySelector(e(t2)) : null, o = (t2) => {
        if (!n(t2) || 0 === t2.getClientRects().length)
          return false;
        const e2 = "visible" === getComputedStyle(t2).getPropertyValue("visibility"), i2 = t2.closest("details:not([open])");
        if (!i2)
          return e2;
        if (i2 !== t2) {
          const e3 = t2.closest("summary");
          if (e3 && e3.parentNode !== i2)
            return false;
          if (null === e3)
            return false;
        }
        return e2;
      }, r = (t2) => !t2 || t2.nodeType !== Node.ELEMENT_NODE || !!t2.classList.contains("disabled") || (void 0 !== t2.disabled ? t2.disabled : t2.hasAttribute("disabled") && "false" !== t2.getAttribute("disabled")), a = (t2) => {
        if (!document.documentElement.attachShadow)
          return null;
        if ("function" == typeof t2.getRootNode) {
          const e2 = t2.getRootNode();
          return e2 instanceof ShadowRoot ? e2 : null;
        }
        return t2 instanceof ShadowRoot ? t2 : t2.parentNode ? a(t2.parentNode) : null;
      }, l = () => {
      }, c = (t2) => {
        t2.offsetHeight;
      }, h = () => window.jQuery && !document.body.hasAttribute("data-coreui-no-jquery") ? window.jQuery : null, u = [], d = () => "rtl" === document.documentElement.dir, f = (t2) => {
        var e2;
        e2 = () => {
          const e3 = h();
          if (e3) {
            const i2 = t2.NAME, n2 = e3.fn[i2];
            e3.fn[i2] = t2.jQueryInterface, e3.fn[i2].Constructor = t2, e3.fn[i2].noConflict = () => (e3.fn[i2] = n2, t2.jQueryInterface);
          }
        }, "loading" === document.readyState ? (u.length || document.addEventListener("DOMContentLoaded", () => {
          for (const t3 of u)
            t3();
        }), u.push(e2)) : e2();
      }, p = (t2, e2 = [], i2 = t2) => "function" == typeof t2 ? t2(...e2) : i2, g = (e2, n2, s2 = true) => {
        if (!s2)
          return void p(e2);
        const o2 = ((t2) => {
          if (!t2)
            return 0;
          let { transitionDuration: e3, transitionDelay: i2 } = window.getComputedStyle(t2);
          const n3 = Number.parseFloat(e3), s3 = Number.parseFloat(i2);
          return n3 || s3 ? (e3 = e3.split(",")[0], i2 = i2.split(",")[0], 1e3 * (Number.parseFloat(e3) + Number.parseFloat(i2))) : 0;
        })(n2) + 5;
        let r2 = false;
        const a2 = ({ target: i2 }) => {
          i2 === n2 && (r2 = true, n2.removeEventListener(t, a2), p(e2));
        };
        n2.addEventListener(t, a2), setTimeout(() => {
          r2 || i(n2);
        }, o2);
      }, m = (t2, e2, i2, n2) => {
        const s2 = t2.length;
        let o2 = t2.indexOf(e2);
        return -1 === o2 ? !i2 && n2 ? t2[s2 - 1] : t2[0] : (o2 += i2 ? 1 : -1, n2 && (o2 = (o2 + s2) % s2), t2[Math.max(0, Math.min(o2, s2 - 1))]);
      }, _ = /[^.]*(?=\..*)\.|.*/, b = /\..*/, v = /::\d+$/, y = {};
      let w = 1;
      const A = { mouseenter: "mouseover", mouseleave: "mouseout" }, E = /* @__PURE__ */ new Set(["click", "dblclick", "mouseup", "mousedown", "contextmenu", "mousewheel", "DOMMouseScroll", "mouseover", "mouseout", "mousemove", "selectstart", "selectend", "keydown", "keypress", "keyup", "orientationchange", "touchstart", "touchmove", "touchend", "touchcancel", "pointerdown", "pointermove", "pointerup", "pointerleave", "pointercancel", "gesturestart", "gesturechange", "gestureend", "focus", "blur", "change", "reset", "select", "submit", "focusin", "focusout", "load", "unload", "beforeunload", "resize", "move", "DOMContentLoaded", "readystatechange", "error", "abort", "scroll"]);
      function C(t2, e2) {
        return e2 && `${e2}::${w++}` || t2.uidEvent || w++;
      }
      function T(t2) {
        const e2 = C(t2);
        return t2.uidEvent = e2, y[e2] = y[e2] || {}, y[e2];
      }
      function O(t2, e2, i2 = null) {
        return Object.values(t2).find((t3) => t3.callable === e2 && t3.delegationSelector === i2);
      }
      function k(t2, e2, i2) {
        const n2 = "string" == typeof e2, s2 = n2 ? i2 : e2 || i2;
        let o2 = S(t2);
        return E.has(o2) || (o2 = t2), [n2, s2, o2];
      }
      function x(t2, e2, i2, n2, s2) {
        if ("string" != typeof e2 || !t2)
          return;
        let [o2, r2, a2] = k(e2, i2, n2);
        if (e2 in A) {
          const t3 = (t4) => function(e3) {
            if (!e3.relatedTarget || e3.relatedTarget !== e3.delegateTarget && !e3.delegateTarget.contains(e3.relatedTarget))
              return t4.call(this, e3);
          };
          r2 = t3(r2);
        }
        const l2 = T(t2), c2 = l2[a2] || (l2[a2] = {}), h2 = O(c2, r2, o2 ? i2 : null);
        if (h2)
          return void (h2.oneOff = h2.oneOff && s2);
        const u2 = C(r2, e2.replace(_, "")), d2 = o2 ? function(t3, e3, i3) {
          return function n3(s3) {
            const o3 = t3.querySelectorAll(e3);
            for (let { target: r3 } = s3; r3 && r3 !== this; r3 = r3.parentNode)
              for (const a3 of o3)
                if (a3 === r3)
                  return I(s3, { delegateTarget: r3 }), n3.oneOff && N.off(t3, s3.type, e3, i3), i3.apply(r3, [s3]);
          };
        }(t2, i2, r2) : function(t3, e3) {
          return function i3(n3) {
            return I(n3, { delegateTarget: t3 }), i3.oneOff && N.off(t3, n3.type, e3), e3.apply(t3, [n3]);
          };
        }(t2, r2);
        d2.delegationSelector = o2 ? i2 : null, d2.callable = r2, d2.oneOff = s2, d2.uidEvent = u2, c2[u2] = d2, t2.addEventListener(a2, d2, o2);
      }
      function L(t2, e2, i2, n2, s2) {
        const o2 = O(e2[i2], n2, s2);
        o2 && (t2.removeEventListener(i2, o2, Boolean(s2)), delete e2[i2][o2.uidEvent]);
      }
      function D(t2, e2, i2, n2) {
        const s2 = e2[i2] || {};
        for (const [o2, r2] of Object.entries(s2))
          o2.includes(n2) && L(t2, e2, i2, r2.callable, r2.delegationSelector);
      }
      function S(t2) {
        return t2 = t2.replace(b, ""), A[t2] || t2;
      }
      const N = { on(t2, e2, i2, n2) {
        x(t2, e2, i2, n2, false);
      }, one(t2, e2, i2, n2) {
        x(t2, e2, i2, n2, true);
      }, off(t2, e2, i2, n2) {
        if ("string" != typeof e2 || !t2)
          return;
        const [s2, o2, r2] = k(e2, i2, n2), a2 = r2 !== e2, l2 = T(t2), c2 = l2[r2] || {}, h2 = e2.startsWith(".");
        if (void 0 === o2) {
          if (h2)
            for (const i3 of Object.keys(l2))
              D(t2, l2, i3, e2.slice(1));
          for (const [i3, n3] of Object.entries(c2)) {
            const s3 = i3.replace(v, "");
            a2 && !e2.includes(s3) || L(t2, l2, r2, n3.callable, n3.delegationSelector);
          }
        } else {
          if (!Object.keys(c2).length)
            return;
          L(t2, l2, r2, o2, s2 ? i2 : null);
        }
      }, trigger(t2, e2, i2) {
        if ("string" != typeof e2 || !t2)
          return null;
        const n2 = h();
        let s2 = null, o2 = true, r2 = true, a2 = false;
        e2 !== S(e2) && n2 && (s2 = n2.Event(e2, i2), n2(t2).trigger(s2), o2 = !s2.isPropagationStopped(), r2 = !s2.isImmediatePropagationStopped(), a2 = s2.isDefaultPrevented());
        let l2 = new Event(e2, { bubbles: o2, cancelable: true });
        return l2 = I(l2, i2), a2 && l2.preventDefault(), r2 && t2.dispatchEvent(l2), l2.defaultPrevented && s2 && s2.preventDefault(), l2;
      } };
      function I(t2, e2 = {}) {
        for (const [i2, n2] of Object.entries(e2))
          try {
            t2[i2] = n2;
          } catch (e3) {
            Object.defineProperty(t2, i2, { configurable: true, get: () => n2 });
          }
        return t2;
      }
      const M = /* @__PURE__ */ new Map(), P = { set(t2, e2, i2) {
        M.has(t2) || M.set(t2, /* @__PURE__ */ new Map());
        const n2 = M.get(t2);
        n2.has(e2) || 0 === n2.size ? n2.set(e2, i2) : console.error(`Bootstrap doesn't allow more than one instance per element. Bound instance: ${Array.from(n2.keys())[0]}.`);
      }, get: (t2, e2) => M.has(t2) && M.get(t2).get(e2) || null, remove(t2, e2) {
        if (!M.has(t2))
          return;
        const i2 = M.get(t2);
        i2.delete(e2), 0 === i2.size && M.delete(t2);
      } };
      function j(t2) {
        if ("true" === t2)
          return true;
        if ("false" === t2)
          return false;
        if (t2 === Number(t2).toString())
          return Number(t2);
        if ("" === t2 || "null" === t2)
          return null;
        if ("string" != typeof t2)
          return t2;
        try {
          return JSON.parse(decodeURIComponent(t2));
        } catch (e2) {
          return t2;
        }
      }
      function $(t2) {
        return t2.replace(/[A-Z]/g, (t3) => `-${t3.toLowerCase()}`);
      }
      const F = { setDataAttribute(t2, e2, i2) {
        t2.setAttribute(`data-coreui-${$(e2)}`, i2);
      }, removeDataAttribute(t2, e2) {
        t2.removeAttribute(`data-coreui-${$(e2)}`);
      }, getDataAttributes(t2) {
        if (!t2)
          return {};
        const e2 = {}, i2 = Object.keys(t2.dataset).filter((t3) => t3.startsWith("coreui") && !t3.startsWith("coreuiConfig"));
        for (const n2 of i2) {
          let i3 = n2.replace(/^coreui/, "");
          i3 = i3.charAt(0).toLowerCase() + i3.slice(1, i3.length), e2[i3] = j(t2.dataset[n2]);
        }
        return e2;
      }, getDataAttribute: (t2, e2) => j(t2.getAttribute(`data-coreui-${$(e2)}`)) };
      class H {
        static get Default() {
          return {};
        }
        static get DefaultType() {
          return {};
        }
        static get NAME() {
          throw new Error('You have to implement the static method "NAME", for each component!');
        }
        _getConfig(t2) {
          return t2 = this._mergeConfigObj(t2), t2 = this._configAfterMerge(t2), this._typeCheckConfig(t2), t2;
        }
        _configAfterMerge(t2) {
          return t2;
        }
        _mergeConfigObj(t2, e2) {
          const i2 = n(e2) ? F.getDataAttribute(e2, "config") : {};
          return { ...this.constructor.Default, ..."object" == typeof i2 ? i2 : {}, ...n(e2) ? F.getDataAttributes(e2) : {}, ..."object" == typeof t2 ? t2 : {} };
        }
        _typeCheckConfig(t2, e2 = this.constructor.DefaultType) {
          for (const [s2, o2] of Object.entries(e2)) {
            const e3 = t2[s2], r2 = n(e3) ? "element" : null == (i2 = e3) ? `${i2}` : Object.prototype.toString.call(i2).match(/\s([a-z]+)/i)[1].toLowerCase();
            if (!new RegExp(o2).test(r2))
              throw new TypeError(`${this.constructor.NAME.toUpperCase()}: Option "${s2}" provided type "${r2}" but expected type "${o2}".`);
          }
          var i2;
        }
      }
      class B extends H {
        constructor(t2, e2) {
          super(), (t2 = s(t2)) && (this._element = t2, this._config = this._getConfig(e2), P.set(this._element, this.constructor.DATA_KEY, this));
        }
        dispose() {
          P.remove(this._element, this.constructor.DATA_KEY), N.off(this._element, this.constructor.EVENT_KEY);
          for (const t2 of Object.getOwnPropertyNames(this))
            this[t2] = null;
        }
        _queueCallback(t2, e2, i2 = true) {
          g(t2, e2, i2);
        }
        _getConfig(t2) {
          return t2 = this._mergeConfigObj(t2, this._element), t2 = this._configAfterMerge(t2), this._typeCheckConfig(t2), t2;
        }
        static getInstance(t2) {
          return P.get(s(t2), this.DATA_KEY);
        }
        static getOrCreateInstance(t2, e2 = {}) {
          return this.getInstance(t2) || new this(t2, "object" == typeof e2 ? e2 : null);
        }
        static get VERSION() {
          return "4.2.6";
        }
        static get DATA_KEY() {
          return `coreui.${this.NAME}`;
        }
        static get EVENT_KEY() {
          return `.${this.DATA_KEY}`;
        }
        static eventName(t2) {
          return `${t2}${this.EVENT_KEY}`;
        }
      }
      const W = (t2) => {
        let i2 = t2.getAttribute("data-coreui-target");
        if (!i2 || "#" === i2) {
          let e2 = t2.getAttribute("href");
          if (!e2 || !e2.includes("#") && !e2.startsWith("."))
            return null;
          e2.includes("#") && !e2.startsWith("#") && (e2 = `#${e2.split("#")[1]}`), i2 = e2 && "#" !== e2 ? e2.trim() : null;
        }
        return e(i2);
      }, z = { find: (t2, e2 = document.documentElement) => [].concat(...Element.prototype.querySelectorAll.call(e2, t2)), findOne: (t2, e2 = document.documentElement) => Element.prototype.querySelector.call(e2, t2), children: (t2, e2) => [].concat(...t2.children).filter((t3) => t3.matches(e2)), parents(t2, e2) {
        const i2 = [];
        let n2 = t2.parentNode.closest(e2);
        for (; n2; )
          i2.push(n2), n2 = n2.parentNode.closest(e2);
        return i2;
      }, prev(t2, e2) {
        let i2 = t2.previousElementSibling;
        for (; i2; ) {
          if (i2.matches(e2))
            return [i2];
          i2 = i2.previousElementSibling;
        }
        return [];
      }, next(t2, e2) {
        let i2 = t2.nextElementSibling;
        for (; i2; ) {
          if (i2.matches(e2))
            return [i2];
          i2 = i2.nextElementSibling;
        }
        return [];
      }, focusableChildren(t2) {
        const e2 = ["a", "button", "input", "textarea", "select", "details", "[tabindex]", '[contenteditable="true"]'].map((t3) => `${t3}:not([tabindex^="-"])`).join(",");
        return this.find(e2, t2).filter((t3) => !r(t3) && o(t3));
      }, getSelectorFromElement(t2) {
        const e2 = W(t2);
        return e2 && z.findOne(e2) ? e2 : null;
      }, getElementFromSelector(t2) {
        const e2 = W(t2);
        return e2 ? z.findOne(e2) : null;
      }, getMultipleElementsFromSelector(t2) {
        const e2 = W(t2);
        return e2 ? z.find(e2) : [];
      } }, q = (t2, e2 = "hide") => {
        const i2 = `click.dismiss${t2.EVENT_KEY}`, n2 = t2.NAME;
        N.on(document, i2, `[data-coreui-dismiss="${n2}"]`, function(i3) {
          if (["A", "AREA"].includes(this.tagName) && i3.preventDefault(), r(this))
            return;
          const s2 = z.getElementFromSelector(this) || this.closest(`.${n2}`);
          t2.getOrCreateInstance(s2)[e2]();
        });
      };
      class R extends B {
        static get NAME() {
          return "alert";
        }
        close() {
          if (N.trigger(this._element, "close.coreui.alert").defaultPrevented)
            return;
          this._element.classList.remove("show");
          const t2 = this._element.classList.contains("fade");
          this._queueCallback(() => this._destroyElement(), this._element, t2);
        }
        _destroyElement() {
          this._element.remove(), N.trigger(this._element, "closed.coreui.alert"), this.dispose();
        }
        static jQueryInterface(t2) {
          return this.each(function() {
            const e2 = R.getOrCreateInstance(this);
            if ("string" == typeof t2) {
              if (void 0 === e2[t2] || t2.startsWith("_") || "constructor" === t2)
                throw new TypeError(`No method named "${t2}"`);
              e2[t2](this);
            }
          });
        }
      }
      q(R, "close"), f(R);
      const V = '[data-coreui-toggle="button"]';
      class U extends B {
        static get NAME() {
          return "button";
        }
        toggle() {
          this._element.setAttribute("aria-pressed", this._element.classList.toggle("active"));
        }
        static jQueryInterface(t2) {
          return this.each(function() {
            const e2 = U.getOrCreateInstance(this);
            "toggle" === t2 && e2[t2]();
          });
        }
      }
      N.on(document, "click.coreui.button.data-api", V, (t2) => {
        t2.preventDefault();
        const e2 = t2.target.closest(V);
        U.getOrCreateInstance(e2).toggle();
      }), f(U);
      const K = { endCallback: null, leftCallback: null, rightCallback: null }, Q = { endCallback: "(function|null)", leftCallback: "(function|null)", rightCallback: "(function|null)" };
      class Y extends H {
        constructor(t2, e2) {
          super(), this._element = t2, t2 && Y.isSupported() && (this._config = this._getConfig(e2), this._deltaX = 0, this._supportPointerEvents = Boolean(window.PointerEvent), this._initEvents());
        }
        static get Default() {
          return K;
        }
        static get DefaultType() {
          return Q;
        }
        static get NAME() {
          return "swipe";
        }
        dispose() {
          N.off(this._element, ".coreui.swipe");
        }
        _start(t2) {
          this._supportPointerEvents ? this._eventIsPointerPenTouch(t2) && (this._deltaX = t2.clientX) : this._deltaX = t2.touches[0].clientX;
        }
        _end(t2) {
          this._eventIsPointerPenTouch(t2) && (this._deltaX = t2.clientX - this._deltaX), this._handleSwipe(), p(this._config.endCallback);
        }
        _move(t2) {
          this._deltaX = t2.touches && t2.touches.length > 1 ? 0 : t2.touches[0].clientX - this._deltaX;
        }
        _handleSwipe() {
          const t2 = Math.abs(this._deltaX);
          if (t2 <= 40)
            return;
          const e2 = t2 / this._deltaX;
          this._deltaX = 0, e2 && p(e2 > 0 ? this._config.rightCallback : this._config.leftCallback);
        }
        _initEvents() {
          this._supportPointerEvents ? (N.on(this._element, "pointerdown.coreui.swipe", (t2) => this._start(t2)), N.on(this._element, "pointerup.coreui.swipe", (t2) => this._end(t2)), this._element.classList.add("pointer-event")) : (N.on(this._element, "touchstart.coreui.swipe", (t2) => this._start(t2)), N.on(this._element, "touchmove.coreui.swipe", (t2) => this._move(t2)), N.on(this._element, "touchend.coreui.swipe", (t2) => this._end(t2)));
        }
        _eventIsPointerPenTouch(t2) {
          return this._supportPointerEvents && ("pen" === t2.pointerType || "touch" === t2.pointerType);
        }
        static isSupported() {
          return "ontouchstart" in document.documentElement || navigator.maxTouchPoints > 0;
        }
      }
      const X = ".coreui.carousel", G = "next", J = "prev", Z = "left", tt = "right", et = `slide${X}`, it = `slid${X}`, nt = `keydown${X}`, st = `mouseenter${X}`, ot = `mouseleave${X}`, rt = `dragstart${X}`, at = `load${X}.data-api`, lt = `click${X}.data-api`, ct = "carousel", ht = "active", ut = { ArrowLeft: tt, ArrowRight: Z }, dt = { interval: 5e3, keyboard: true, pause: "hover", ride: false, touch: true, wrap: true }, ft = { interval: "(number|boolean)", keyboard: "boolean", pause: "(string|boolean)", ride: "(boolean|string)", touch: "boolean", wrap: "boolean" };
      class pt extends B {
        constructor(t2, e2) {
          super(t2, e2), this._interval = null, this._activeElement = null, this._isSliding = false, this.touchTimeout = null, this._swipeHelper = null, this._indicatorsElement = z.findOne(".carousel-indicators", this._element), this._addEventListeners(), this._config.ride === ct && this.cycle();
        }
        static get Default() {
          return dt;
        }
        static get DefaultType() {
          return ft;
        }
        static get NAME() {
          return "carousel";
        }
        next() {
          this._slide(G);
        }
        nextWhenVisible() {
          !document.hidden && o(this._element) && this.next();
        }
        prev() {
          this._slide(J);
        }
        pause() {
          this._isSliding && i(this._element), this._clearInterval();
        }
        cycle() {
          this._clearInterval(), this._updateInterval(), this._interval = setInterval(() => this.nextWhenVisible(), this._config.interval);
        }
        _maybeEnableCycle() {
          this._config.ride && (this._isSliding ? N.one(this._element, it, () => this.cycle()) : this.cycle());
        }
        to(t2) {
          const e2 = this._getItems();
          if (t2 > e2.length - 1 || t2 < 0)
            return;
          if (this._isSliding)
            return void N.one(this._element, it, () => this.to(t2));
          const i2 = this._getItemIndex(this._getActive());
          if (i2 === t2)
            return;
          const n2 = t2 > i2 ? G : J;
          this._slide(n2, e2[t2]);
        }
        dispose() {
          this._swipeHelper && this._swipeHelper.dispose(), super.dispose();
        }
        _configAfterMerge(t2) {
          return t2.defaultInterval = t2.interval, t2;
        }
        _addEventListeners() {
          this._config.keyboard && N.on(this._element, nt, (t2) => this._keydown(t2)), "hover" === this._config.pause && (N.on(this._element, st, () => this.pause()), N.on(this._element, ot, () => this._maybeEnableCycle())), this._config.touch && Y.isSupported() && this._addTouchEventListeners();
        }
        _addTouchEventListeners() {
          for (const t3 of z.find(".carousel-item img", this._element))
            N.on(t3, rt, (t4) => t4.preventDefault());
          const t2 = { leftCallback: () => this._slide(this._directionToOrder(Z)), rightCallback: () => this._slide(this._directionToOrder(tt)), endCallback: () => {
            "hover" === this._config.pause && (this.pause(), this.touchTimeout && clearTimeout(this.touchTimeout), this.touchTimeout = setTimeout(() => this._maybeEnableCycle(), 500 + this._config.interval));
          } };
          this._swipeHelper = new Y(this._element, t2);
        }
        _keydown(t2) {
          if (/input|textarea/i.test(t2.target.tagName))
            return;
          const e2 = ut[t2.key];
          e2 && (t2.preventDefault(), this._slide(this._directionToOrder(e2)));
        }
        _getItemIndex(t2) {
          return this._getItems().indexOf(t2);
        }
        _setActiveIndicatorElement(t2) {
          if (!this._indicatorsElement)
            return;
          const e2 = z.findOne(".active", this._indicatorsElement);
          e2.classList.remove(ht), e2.removeAttribute("aria-current");
          const i2 = z.findOne(`[data-coreui-slide-to="${t2}"]`, this._indicatorsElement);
          i2 && (i2.classList.add(ht), i2.setAttribute("aria-current", "true"));
        }
        _updateInterval() {
          const t2 = this._activeElement || this._getActive();
          if (!t2)
            return;
          const e2 = Number.parseInt(t2.getAttribute("data-coreui-interval"), 10);
          this._config.interval = e2 || this._config.defaultInterval;
        }
        _slide(t2, e2 = null) {
          if (this._isSliding)
            return;
          const i2 = this._getActive(), n2 = t2 === G, s2 = e2 || m(this._getItems(), i2, n2, this._config.wrap);
          if (s2 === i2)
            return;
          const o2 = this._getItemIndex(s2), r2 = (e3) => N.trigger(this._element, e3, { relatedTarget: s2, direction: this._orderToDirection(t2), from: this._getItemIndex(i2), to: o2 });
          if (r2(et).defaultPrevented)
            return;
          if (!i2 || !s2)
            return;
          const a2 = Boolean(this._interval);
          this.pause(), this._isSliding = true, this._setActiveIndicatorElement(o2), this._activeElement = s2;
          const l2 = n2 ? "carousel-item-start" : "carousel-item-end", h2 = n2 ? "carousel-item-next" : "carousel-item-prev";
          s2.classList.add(h2), c(s2), i2.classList.add(l2), s2.classList.add(l2), this._queueCallback(() => {
            s2.classList.remove(l2, h2), s2.classList.add(ht), i2.classList.remove(ht, h2, l2), this._isSliding = false, r2(it);
          }, i2, this._isAnimated()), a2 && this.cycle();
        }
        _isAnimated() {
          return this._element.classList.contains("slide");
        }
        _getActive() {
          return z.findOne(".active.carousel-item", this._element);
        }
        _getItems() {
          return z.find(".carousel-item", this._element);
        }
        _clearInterval() {
          this._interval && (clearInterval(this._interval), this._interval = null);
        }
        _directionToOrder(t2) {
          return d() ? t2 === Z ? J : G : t2 === Z ? G : J;
        }
        _orderToDirection(t2) {
          return d() ? t2 === J ? Z : tt : t2 === J ? tt : Z;
        }
        static jQueryInterface(t2) {
          return this.each(function() {
            const e2 = pt.getOrCreateInstance(this, t2);
            if ("number" != typeof t2) {
              if ("string" == typeof t2) {
                if (void 0 === e2[t2] || t2.startsWith("_") || "constructor" === t2)
                  throw new TypeError(`No method named "${t2}"`);
                e2[t2]();
              }
            } else
              e2.to(t2);
          });
        }
      }
      N.on(document, lt, "[data-coreui-slide], [data-coreui-slide-to]", function(t2) {
        const e2 = z.getElementFromSelector(this);
        if (!e2 || !e2.classList.contains(ct))
          return;
        t2.preventDefault();
        const i2 = pt.getOrCreateInstance(e2), n2 = this.getAttribute("data-coreui-slide-to");
        return n2 ? (i2.to(n2), void i2._maybeEnableCycle()) : "next" === F.getDataAttribute(this, "slide") ? (i2.next(), void i2._maybeEnableCycle()) : (i2.prev(), void i2._maybeEnableCycle());
      }), N.on(window, at, () => {
        const t2 = z.find('[data-coreui-ride="carousel"]');
        for (const e2 of t2)
          pt.getOrCreateInstance(e2);
      }), f(pt);
      const gt = ".coreui.collapse", mt = `show${gt}`, _t = `shown${gt}`, bt = `hide${gt}`, vt = `hidden${gt}`, yt = `click${gt}.data-api`, wt = "show", At = "collapse", Et = "collapsing", Ct = '[data-coreui-toggle="collapse"]', Tt = { parent: null, toggle: true }, Ot = { parent: "(null|element)", toggle: "boolean" };
      class kt extends B {
        constructor(t2, e2) {
          super(t2, e2), this._isTransitioning = false, this._triggerArray = [];
          const i2 = z.find(Ct);
          for (const t3 of i2) {
            const e3 = z.getSelectorFromElement(t3), i3 = z.find(e3).filter((t4) => t4 === this._element);
            null !== e3 && i3.length && this._triggerArray.push(t3);
          }
          this._initializeChildren(), this._config.parent || this._addAriaAndCollapsedClass(this._triggerArray, this._isShown()), this._config.toggle && this.toggle();
        }
        static get Default() {
          return Tt;
        }
        static get DefaultType() {
          return Ot;
        }
        static get NAME() {
          return "collapse";
        }
        toggle() {
          this._isShown() ? this.hide() : this.show();
        }
        show() {
          if (this._isTransitioning || this._isShown())
            return;
          let t2 = [];
          if (this._config.parent && (t2 = this._getFirstLevelChildren(".collapse.show, .collapse.collapsing").filter((t3) => t3 !== this._element).map((t3) => kt.getOrCreateInstance(t3, { toggle: false }))), t2.length && t2[0]._isTransitioning)
            return;
          if (N.trigger(this._element, mt).defaultPrevented)
            return;
          for (const e3 of t2)
            e3.hide();
          const e2 = this._getDimension();
          this._element.classList.remove(At), this._element.classList.add(Et), this._element.style[e2] = 0, this._addAriaAndCollapsedClass(this._triggerArray, true), this._isTransitioning = true;
          const i2 = `scroll${e2[0].toUpperCase() + e2.slice(1)}`;
          this._queueCallback(() => {
            this._isTransitioning = false, this._element.classList.remove(Et), this._element.classList.add(At, wt), this._element.style[e2] = "", N.trigger(this._element, _t);
          }, this._element, true), this._element.style[e2] = `${this._element[i2]}px`;
        }
        hide() {
          if (this._isTransitioning || !this._isShown())
            return;
          if (N.trigger(this._element, bt).defaultPrevented)
            return;
          const t2 = this._getDimension();
          this._element.style[t2] = `${this._element.getBoundingClientRect()[t2]}px`, c(this._element), this._element.classList.add(Et), this._element.classList.remove(At, wt);
          for (const t3 of this._triggerArray) {
            const e2 = z.getElementFromSelector(t3);
            e2 && !this._isShown(e2) && this._addAriaAndCollapsedClass([t3], false);
          }
          this._isTransitioning = true, this._element.style[t2] = "", this._queueCallback(() => {
            this._isTransitioning = false, this._element.classList.remove(Et), this._element.classList.add(At), N.trigger(this._element, vt);
          }, this._element, true);
        }
        _isShown(t2 = this._element) {
          return t2.classList.contains(wt);
        }
        _configAfterMerge(t2) {
          return t2.toggle = Boolean(t2.toggle), t2.parent = s(t2.parent), t2;
        }
        _getDimension() {
          return this._element.classList.contains("collapse-horizontal") ? "width" : "height";
        }
        _initializeChildren() {
          if (!this._config.parent)
            return;
          const t2 = this._getFirstLevelChildren(Ct);
          for (const e2 of t2) {
            const t3 = z.getElementFromSelector(e2);
            t3 && this._addAriaAndCollapsedClass([e2], this._isShown(t3));
          }
        }
        _getFirstLevelChildren(t2) {
          const e2 = z.find(":scope .collapse .collapse", this._config.parent);
          return z.find(t2, this._config.parent).filter((t3) => !e2.includes(t3));
        }
        _addAriaAndCollapsedClass(t2, e2) {
          if (t2.length)
            for (const i2 of t2)
              i2.classList.toggle("collapsed", !e2), i2.setAttribute("aria-expanded", e2);
        }
        static jQueryInterface(t2) {
          const e2 = {};
          return "string" == typeof t2 && /show|hide/.test(t2) && (e2.toggle = false), this.each(function() {
            const i2 = kt.getOrCreateInstance(this, e2);
            if ("string" == typeof t2) {
              if (void 0 === i2[t2])
                throw new TypeError(`No method named "${t2}"`);
              i2[t2]();
            }
          });
        }
      }
      N.on(document, yt, Ct, function(t2) {
        ("A" === t2.target.tagName || t2.delegateTarget && "A" === t2.delegateTarget.tagName) && t2.preventDefault();
        for (const t3 of z.getMultipleElementsFromSelector(this))
          kt.getOrCreateInstance(t3, { toggle: false }).toggle();
      }), f(kt);
      var xt = "top", Lt = "bottom", Dt = "right", St = "left", Nt = "auto", It = [xt, Lt, Dt, St], Mt = "start", Pt = "end", jt = "clippingParents", $t = "viewport", Ft = "popper", Ht = "reference", Bt = It.reduce(function(t2, e2) {
        return t2.concat([e2 + "-" + Mt, e2 + "-" + Pt]);
      }, []), Wt = [].concat(It, [Nt]).reduce(function(t2, e2) {
        return t2.concat([e2, e2 + "-" + Mt, e2 + "-" + Pt]);
      }, []), zt = "beforeRead", qt = "read", Rt = "afterRead", Vt = "beforeMain", Ut = "main", Kt = "afterMain", Qt = "beforeWrite", Yt = "write", Xt = "afterWrite", Gt = [zt, qt, Rt, Vt, Ut, Kt, Qt, Yt, Xt];
      function Jt(t2) {
        return t2 ? (t2.nodeName || "").toLowerCase() : null;
      }
      function Zt(t2) {
        if (null == t2)
          return window;
        if ("[object Window]" !== t2.toString()) {
          var e2 = t2.ownerDocument;
          return e2 && e2.defaultView || window;
        }
        return t2;
      }
      function te(t2) {
        return t2 instanceof Zt(t2).Element || t2 instanceof Element;
      }
      function ee(t2) {
        return t2 instanceof Zt(t2).HTMLElement || t2 instanceof HTMLElement;
      }
      function ie(t2) {
        return "undefined" != typeof ShadowRoot && (t2 instanceof Zt(t2).ShadowRoot || t2 instanceof ShadowRoot);
      }
      const ne = { name: "applyStyles", enabled: true, phase: "write", fn: function(t2) {
        var e2 = t2.state;
        Object.keys(e2.elements).forEach(function(t3) {
          var i2 = e2.styles[t3] || {}, n2 = e2.attributes[t3] || {}, s2 = e2.elements[t3];
          ee(s2) && Jt(s2) && (Object.assign(s2.style, i2), Object.keys(n2).forEach(function(t4) {
            var e3 = n2[t4];
            false === e3 ? s2.removeAttribute(t4) : s2.setAttribute(t4, true === e3 ? "" : e3);
          }));
        });
      }, effect: function(t2) {
        var e2 = t2.state, i2 = { popper: { position: e2.options.strategy, left: "0", top: "0", margin: "0" }, arrow: { position: "absolute" }, reference: {} };
        return Object.assign(e2.elements.popper.style, i2.popper), e2.styles = i2, e2.elements.arrow && Object.assign(e2.elements.arrow.style, i2.arrow), function() {
          Object.keys(e2.elements).forEach(function(t3) {
            var n2 = e2.elements[t3], s2 = e2.attributes[t3] || {}, o2 = Object.keys(e2.styles.hasOwnProperty(t3) ? e2.styles[t3] : i2[t3]).reduce(function(t4, e3) {
              return t4[e3] = "", t4;
            }, {});
            ee(n2) && Jt(n2) && (Object.assign(n2.style, o2), Object.keys(s2).forEach(function(t4) {
              n2.removeAttribute(t4);
            }));
          });
        };
      }, requires: ["computeStyles"] };
      function se(t2) {
        return t2.split("-")[0];
      }
      var oe = Math.max, re = Math.min, ae = Math.round;
      function le() {
        var t2 = navigator.userAgentData;
        return null != t2 && t2.brands ? t2.brands.map(function(t3) {
          return t3.brand + "/" + t3.version;
        }).join(" ") : navigator.userAgent;
      }
      function ce() {
        return !/^((?!chrome|android).)*safari/i.test(le());
      }
      function he(t2, e2, i2) {
        void 0 === e2 && (e2 = false), void 0 === i2 && (i2 = false);
        var n2 = t2.getBoundingClientRect(), s2 = 1, o2 = 1;
        e2 && ee(t2) && (s2 = t2.offsetWidth > 0 && ae(n2.width) / t2.offsetWidth || 1, o2 = t2.offsetHeight > 0 && ae(n2.height) / t2.offsetHeight || 1);
        var r2 = (te(t2) ? Zt(t2) : window).visualViewport, a2 = !ce() && i2, l2 = (n2.left + (a2 && r2 ? r2.offsetLeft : 0)) / s2, c2 = (n2.top + (a2 && r2 ? r2.offsetTop : 0)) / o2, h2 = n2.width / s2, u2 = n2.height / o2;
        return { width: h2, height: u2, top: c2, right: l2 + h2, bottom: c2 + u2, left: l2, x: l2, y: c2 };
      }
      function ue(t2) {
        var e2 = he(t2), i2 = t2.offsetWidth, n2 = t2.offsetHeight;
        return Math.abs(e2.width - i2) <= 1 && (i2 = e2.width), Math.abs(e2.height - n2) <= 1 && (n2 = e2.height), { x: t2.offsetLeft, y: t2.offsetTop, width: i2, height: n2 };
      }
      function de(t2, e2) {
        var i2 = e2.getRootNode && e2.getRootNode();
        if (t2.contains(e2))
          return true;
        if (i2 && ie(i2)) {
          var n2 = e2;
          do {
            if (n2 && t2.isSameNode(n2))
              return true;
            n2 = n2.parentNode || n2.host;
          } while (n2);
        }
        return false;
      }
      function fe(t2) {
        return Zt(t2).getComputedStyle(t2);
      }
      function pe(t2) {
        return ["table", "td", "th"].indexOf(Jt(t2)) >= 0;
      }
      function ge(t2) {
        return ((te(t2) ? t2.ownerDocument : t2.document) || window.document).documentElement;
      }
      function me(t2) {
        return "html" === Jt(t2) ? t2 : t2.assignedSlot || t2.parentNode || (ie(t2) ? t2.host : null) || ge(t2);
      }
      function _e(t2) {
        return ee(t2) && "fixed" !== fe(t2).position ? t2.offsetParent : null;
      }
      function be(t2) {
        for (var e2 = Zt(t2), i2 = _e(t2); i2 && pe(i2) && "static" === fe(i2).position; )
          i2 = _e(i2);
        return i2 && ("html" === Jt(i2) || "body" === Jt(i2) && "static" === fe(i2).position) ? e2 : i2 || function(t3) {
          var e3 = /firefox/i.test(le());
          if (/Trident/i.test(le()) && ee(t3) && "fixed" === fe(t3).position)
            return null;
          var i3 = me(t3);
          for (ie(i3) && (i3 = i3.host); ee(i3) && ["html", "body"].indexOf(Jt(i3)) < 0; ) {
            var n2 = fe(i3);
            if ("none" !== n2.transform || "none" !== n2.perspective || "paint" === n2.contain || -1 !== ["transform", "perspective"].indexOf(n2.willChange) || e3 && "filter" === n2.willChange || e3 && n2.filter && "none" !== n2.filter)
              return i3;
            i3 = i3.parentNode;
          }
          return null;
        }(t2) || e2;
      }
      function ve(t2) {
        return ["top", "bottom"].indexOf(t2) >= 0 ? "x" : "y";
      }
      function ye(t2, e2, i2) {
        return oe(t2, re(e2, i2));
      }
      function we(t2) {
        return Object.assign({}, { top: 0, right: 0, bottom: 0, left: 0 }, t2);
      }
      function Ae(t2, e2) {
        return e2.reduce(function(e3, i2) {
          return e3[i2] = t2, e3;
        }, {});
      }
      const Ee = { name: "arrow", enabled: true, phase: "main", fn: function(t2) {
        var e2, i2 = t2.state, n2 = t2.name, s2 = t2.options, o2 = i2.elements.arrow, r2 = i2.modifiersData.popperOffsets, a2 = se(i2.placement), l2 = ve(a2), c2 = [St, Dt].indexOf(a2) >= 0 ? "height" : "width";
        if (o2 && r2) {
          var h2 = function(t3, e3) {
            return we("number" != typeof (t3 = "function" == typeof t3 ? t3(Object.assign({}, e3.rects, { placement: e3.placement })) : t3) ? t3 : Ae(t3, It));
          }(s2.padding, i2), u2 = ue(o2), d2 = "y" === l2 ? xt : St, f2 = "y" === l2 ? Lt : Dt, p2 = i2.rects.reference[c2] + i2.rects.reference[l2] - r2[l2] - i2.rects.popper[c2], g2 = r2[l2] - i2.rects.reference[l2], m2 = be(o2), _2 = m2 ? "y" === l2 ? m2.clientHeight || 0 : m2.clientWidth || 0 : 0, b2 = p2 / 2 - g2 / 2, v2 = h2[d2], y2 = _2 - u2[c2] - h2[f2], w2 = _2 / 2 - u2[c2] / 2 + b2, A2 = ye(v2, w2, y2), E2 = l2;
          i2.modifiersData[n2] = ((e2 = {})[E2] = A2, e2.centerOffset = A2 - w2, e2);
        }
      }, effect: function(t2) {
        var e2 = t2.state, i2 = t2.options.element, n2 = void 0 === i2 ? "[data-popper-arrow]" : i2;
        null != n2 && ("string" != typeof n2 || (n2 = e2.elements.popper.querySelector(n2))) && de(e2.elements.popper, n2) && (e2.elements.arrow = n2);
      }, requires: ["popperOffsets"], requiresIfExists: ["preventOverflow"] };
      function Ce(t2) {
        return t2.split("-")[1];
      }
      var Te = { top: "auto", right: "auto", bottom: "auto", left: "auto" };
      function Oe(t2) {
        var e2, i2 = t2.popper, n2 = t2.popperRect, s2 = t2.placement, o2 = t2.variation, r2 = t2.offsets, a2 = t2.position, l2 = t2.gpuAcceleration, c2 = t2.adaptive, h2 = t2.roundOffsets, u2 = t2.isFixed, d2 = r2.x, f2 = void 0 === d2 ? 0 : d2, p2 = r2.y, g2 = void 0 === p2 ? 0 : p2, m2 = "function" == typeof h2 ? h2({ x: f2, y: g2 }) : { x: f2, y: g2 };
        f2 = m2.x, g2 = m2.y;
        var _2 = r2.hasOwnProperty("x"), b2 = r2.hasOwnProperty("y"), v2 = St, y2 = xt, w2 = window;
        if (c2) {
          var A2 = be(i2), E2 = "clientHeight", C2 = "clientWidth";
          A2 === Zt(i2) && "static" !== fe(A2 = ge(i2)).position && "absolute" === a2 && (E2 = "scrollHeight", C2 = "scrollWidth"), (s2 === xt || (s2 === St || s2 === Dt) && o2 === Pt) && (y2 = Lt, g2 -= (u2 && A2 === w2 && w2.visualViewport ? w2.visualViewport.height : A2[E2]) - n2.height, g2 *= l2 ? 1 : -1), s2 !== St && (s2 !== xt && s2 !== Lt || o2 !== Pt) || (v2 = Dt, f2 -= (u2 && A2 === w2 && w2.visualViewport ? w2.visualViewport.width : A2[C2]) - n2.width, f2 *= l2 ? 1 : -1);
        }
        var T2, O2 = Object.assign({ position: a2 }, c2 && Te), k2 = true === h2 ? function(t3) {
          var e3 = t3.x, i3 = t3.y, n3 = window.devicePixelRatio || 1;
          return { x: ae(e3 * n3) / n3 || 0, y: ae(i3 * n3) / n3 || 0 };
        }({ x: f2, y: g2 }) : { x: f2, y: g2 };
        return f2 = k2.x, g2 = k2.y, l2 ? Object.assign({}, O2, ((T2 = {})[y2] = b2 ? "0" : "", T2[v2] = _2 ? "0" : "", T2.transform = (w2.devicePixelRatio || 1) <= 1 ? "translate(" + f2 + "px, " + g2 + "px)" : "translate3d(" + f2 + "px, " + g2 + "px, 0)", T2)) : Object.assign({}, O2, ((e2 = {})[y2] = b2 ? g2 + "px" : "", e2[v2] = _2 ? f2 + "px" : "", e2.transform = "", e2));
      }
      const ke = { name: "computeStyles", enabled: true, phase: "beforeWrite", fn: function(t2) {
        var e2 = t2.state, i2 = t2.options, n2 = i2.gpuAcceleration, s2 = void 0 === n2 || n2, o2 = i2.adaptive, r2 = void 0 === o2 || o2, a2 = i2.roundOffsets, l2 = void 0 === a2 || a2, c2 = { placement: se(e2.placement), variation: Ce(e2.placement), popper: e2.elements.popper, popperRect: e2.rects.popper, gpuAcceleration: s2, isFixed: "fixed" === e2.options.strategy };
        null != e2.modifiersData.popperOffsets && (e2.styles.popper = Object.assign({}, e2.styles.popper, Oe(Object.assign({}, c2, { offsets: e2.modifiersData.popperOffsets, position: e2.options.strategy, adaptive: r2, roundOffsets: l2 })))), null != e2.modifiersData.arrow && (e2.styles.arrow = Object.assign({}, e2.styles.arrow, Oe(Object.assign({}, c2, { offsets: e2.modifiersData.arrow, position: "absolute", adaptive: false, roundOffsets: l2 })))), e2.attributes.popper = Object.assign({}, e2.attributes.popper, { "data-popper-placement": e2.placement });
      }, data: {} };
      var xe = { passive: true };
      const Le = { name: "eventListeners", enabled: true, phase: "write", fn: function() {
      }, effect: function(t2) {
        var e2 = t2.state, i2 = t2.instance, n2 = t2.options, s2 = n2.scroll, o2 = void 0 === s2 || s2, r2 = n2.resize, a2 = void 0 === r2 || r2, l2 = Zt(e2.elements.popper), c2 = [].concat(e2.scrollParents.reference, e2.scrollParents.popper);
        return o2 && c2.forEach(function(t3) {
          t3.addEventListener("scroll", i2.update, xe);
        }), a2 && l2.addEventListener("resize", i2.update, xe), function() {
          o2 && c2.forEach(function(t3) {
            t3.removeEventListener("scroll", i2.update, xe);
          }), a2 && l2.removeEventListener("resize", i2.update, xe);
        };
      }, data: {} };
      var De = { left: "right", right: "left", bottom: "top", top: "bottom" };
      function Se(t2) {
        return t2.replace(/left|right|bottom|top/g, function(t3) {
          return De[t3];
        });
      }
      var Ne = { start: "end", end: "start" };
      function Ie(t2) {
        return t2.replace(/start|end/g, function(t3) {
          return Ne[t3];
        });
      }
      function Me(t2) {
        var e2 = Zt(t2);
        return { scrollLeft: e2.pageXOffset, scrollTop: e2.pageYOffset };
      }
      function Pe(t2) {
        return he(ge(t2)).left + Me(t2).scrollLeft;
      }
      function je(t2) {
        var e2 = fe(t2), i2 = e2.overflow, n2 = e2.overflowX, s2 = e2.overflowY;
        return /auto|scroll|overlay|hidden/.test(i2 + s2 + n2);
      }
      function $e(t2) {
        return ["html", "body", "#document"].indexOf(Jt(t2)) >= 0 ? t2.ownerDocument.body : ee(t2) && je(t2) ? t2 : $e(me(t2));
      }
      function Fe(t2, e2) {
        var i2;
        void 0 === e2 && (e2 = []);
        var n2 = $e(t2), s2 = n2 === (null == (i2 = t2.ownerDocument) ? void 0 : i2.body), o2 = Zt(n2), r2 = s2 ? [o2].concat(o2.visualViewport || [], je(n2) ? n2 : []) : n2, a2 = e2.concat(r2);
        return s2 ? a2 : a2.concat(Fe(me(r2)));
      }
      function He(t2) {
        return Object.assign({}, t2, { left: t2.x, top: t2.y, right: t2.x + t2.width, bottom: t2.y + t2.height });
      }
      function Be(t2, e2, i2) {
        return e2 === $t ? He(function(t3, e3) {
          var i3 = Zt(t3), n2 = ge(t3), s2 = i3.visualViewport, o2 = n2.clientWidth, r2 = n2.clientHeight, a2 = 0, l2 = 0;
          if (s2) {
            o2 = s2.width, r2 = s2.height;
            var c2 = ce();
            (c2 || !c2 && "fixed" === e3) && (a2 = s2.offsetLeft, l2 = s2.offsetTop);
          }
          return { width: o2, height: r2, x: a2 + Pe(t3), y: l2 };
        }(t2, i2)) : te(e2) ? function(t3, e3) {
          var i3 = he(t3, false, "fixed" === e3);
          return i3.top = i3.top + t3.clientTop, i3.left = i3.left + t3.clientLeft, i3.bottom = i3.top + t3.clientHeight, i3.right = i3.left + t3.clientWidth, i3.width = t3.clientWidth, i3.height = t3.clientHeight, i3.x = i3.left, i3.y = i3.top, i3;
        }(e2, i2) : He(function(t3) {
          var e3, i3 = ge(t3), n2 = Me(t3), s2 = null == (e3 = t3.ownerDocument) ? void 0 : e3.body, o2 = oe(i3.scrollWidth, i3.clientWidth, s2 ? s2.scrollWidth : 0, s2 ? s2.clientWidth : 0), r2 = oe(i3.scrollHeight, i3.clientHeight, s2 ? s2.scrollHeight : 0, s2 ? s2.clientHeight : 0), a2 = -n2.scrollLeft + Pe(t3), l2 = -n2.scrollTop;
          return "rtl" === fe(s2 || i3).direction && (a2 += oe(i3.clientWidth, s2 ? s2.clientWidth : 0) - o2), { width: o2, height: r2, x: a2, y: l2 };
        }(ge(t2)));
      }
      function We(t2) {
        var e2, i2 = t2.reference, n2 = t2.element, s2 = t2.placement, o2 = s2 ? se(s2) : null, r2 = s2 ? Ce(s2) : null, a2 = i2.x + i2.width / 2 - n2.width / 2, l2 = i2.y + i2.height / 2 - n2.height / 2;
        switch (o2) {
          case xt:
            e2 = { x: a2, y: i2.y - n2.height };
            break;
          case Lt:
            e2 = { x: a2, y: i2.y + i2.height };
            break;
          case Dt:
            e2 = { x: i2.x + i2.width, y: l2 };
            break;
          case St:
            e2 = { x: i2.x - n2.width, y: l2 };
            break;
          default:
            e2 = { x: i2.x, y: i2.y };
        }
        var c2 = o2 ? ve(o2) : null;
        if (null != c2) {
          var h2 = "y" === c2 ? "height" : "width";
          switch (r2) {
            case Mt:
              e2[c2] = e2[c2] - (i2[h2] / 2 - n2[h2] / 2);
              break;
            case Pt:
              e2[c2] = e2[c2] + (i2[h2] / 2 - n2[h2] / 2);
          }
        }
        return e2;
      }
      function ze(t2, e2) {
        void 0 === e2 && (e2 = {});
        var i2 = e2, n2 = i2.placement, s2 = void 0 === n2 ? t2.placement : n2, o2 = i2.strategy, r2 = void 0 === o2 ? t2.strategy : o2, a2 = i2.boundary, l2 = void 0 === a2 ? jt : a2, c2 = i2.rootBoundary, h2 = void 0 === c2 ? $t : c2, u2 = i2.elementContext, d2 = void 0 === u2 ? Ft : u2, f2 = i2.altBoundary, p2 = void 0 !== f2 && f2, g2 = i2.padding, m2 = void 0 === g2 ? 0 : g2, _2 = we("number" != typeof m2 ? m2 : Ae(m2, It)), b2 = d2 === Ft ? Ht : Ft, v2 = t2.rects.popper, y2 = t2.elements[p2 ? b2 : d2], w2 = function(t3, e3, i3, n3) {
          var s3 = "clippingParents" === e3 ? function(t4) {
            var e4 = Fe(me(t4)), i4 = ["absolute", "fixed"].indexOf(fe(t4).position) >= 0 && ee(t4) ? be(t4) : t4;
            return te(i4) ? e4.filter(function(t5) {
              return te(t5) && de(t5, i4) && "body" !== Jt(t5);
            }) : [];
          }(t3) : [].concat(e3), o3 = [].concat(s3, [i3]), r3 = o3[0], a3 = o3.reduce(function(e4, i4) {
            var s4 = Be(t3, i4, n3);
            return e4.top = oe(s4.top, e4.top), e4.right = re(s4.right, e4.right), e4.bottom = re(s4.bottom, e4.bottom), e4.left = oe(s4.left, e4.left), e4;
          }, Be(t3, r3, n3));
          return a3.width = a3.right - a3.left, a3.height = a3.bottom - a3.top, a3.x = a3.left, a3.y = a3.top, a3;
        }(te(y2) ? y2 : y2.contextElement || ge(t2.elements.popper), l2, h2, r2), A2 = he(t2.elements.reference), E2 = We({ reference: A2, element: v2, strategy: "absolute", placement: s2 }), C2 = He(Object.assign({}, v2, E2)), T2 = d2 === Ft ? C2 : A2, O2 = { top: w2.top - T2.top + _2.top, bottom: T2.bottom - w2.bottom + _2.bottom, left: w2.left - T2.left + _2.left, right: T2.right - w2.right + _2.right }, k2 = t2.modifiersData.offset;
        if (d2 === Ft && k2) {
          var x2 = k2[s2];
          Object.keys(O2).forEach(function(t3) {
            var e3 = [Dt, Lt].indexOf(t3) >= 0 ? 1 : -1, i3 = [xt, Lt].indexOf(t3) >= 0 ? "y" : "x";
            O2[t3] += x2[i3] * e3;
          });
        }
        return O2;
      }
      function qe(t2, e2) {
        void 0 === e2 && (e2 = {});
        var i2 = e2, n2 = i2.placement, s2 = i2.boundary, o2 = i2.rootBoundary, r2 = i2.padding, a2 = i2.flipVariations, l2 = i2.allowedAutoPlacements, c2 = void 0 === l2 ? Wt : l2, h2 = Ce(n2), u2 = h2 ? a2 ? Bt : Bt.filter(function(t3) {
          return Ce(t3) === h2;
        }) : It, d2 = u2.filter(function(t3) {
          return c2.indexOf(t3) >= 0;
        });
        0 === d2.length && (d2 = u2);
        var f2 = d2.reduce(function(e3, i3) {
          return e3[i3] = ze(t2, { placement: i3, boundary: s2, rootBoundary: o2, padding: r2 })[se(i3)], e3;
        }, {});
        return Object.keys(f2).sort(function(t3, e3) {
          return f2[t3] - f2[e3];
        });
      }
      const Re = { name: "flip", enabled: true, phase: "main", fn: function(t2) {
        var e2 = t2.state, i2 = t2.options, n2 = t2.name;
        if (!e2.modifiersData[n2]._skip) {
          for (var s2 = i2.mainAxis, o2 = void 0 === s2 || s2, r2 = i2.altAxis, a2 = void 0 === r2 || r2, l2 = i2.fallbackPlacements, c2 = i2.padding, h2 = i2.boundary, u2 = i2.rootBoundary, d2 = i2.altBoundary, f2 = i2.flipVariations, p2 = void 0 === f2 || f2, g2 = i2.allowedAutoPlacements, m2 = e2.options.placement, _2 = se(m2), b2 = l2 || (_2 !== m2 && p2 ? function(t3) {
            if (se(t3) === Nt)
              return [];
            var e3 = Se(t3);
            return [Ie(t3), e3, Ie(e3)];
          }(m2) : [Se(m2)]), v2 = [m2].concat(b2).reduce(function(t3, i3) {
            return t3.concat(se(i3) === Nt ? qe(e2, { placement: i3, boundary: h2, rootBoundary: u2, padding: c2, flipVariations: p2, allowedAutoPlacements: g2 }) : i3);
          }, []), y2 = e2.rects.reference, w2 = e2.rects.popper, A2 = /* @__PURE__ */ new Map(), E2 = true, C2 = v2[0], T2 = 0; T2 < v2.length; T2++) {
            var O2 = v2[T2], k2 = se(O2), x2 = Ce(O2) === Mt, L2 = [xt, Lt].indexOf(k2) >= 0, D2 = L2 ? "width" : "height", S2 = ze(e2, { placement: O2, boundary: h2, rootBoundary: u2, altBoundary: d2, padding: c2 }), N2 = L2 ? x2 ? Dt : St : x2 ? Lt : xt;
            y2[D2] > w2[D2] && (N2 = Se(N2));
            var I2 = Se(N2), M2 = [];
            if (o2 && M2.push(S2[k2] <= 0), a2 && M2.push(S2[N2] <= 0, S2[I2] <= 0), M2.every(function(t3) {
              return t3;
            })) {
              C2 = O2, E2 = false;
              break;
            }
            A2.set(O2, M2);
          }
          if (E2)
            for (var P2 = function(t3) {
              var e3 = v2.find(function(e4) {
                var i3 = A2.get(e4);
                if (i3)
                  return i3.slice(0, t3).every(function(t4) {
                    return t4;
                  });
              });
              if (e3)
                return C2 = e3, "break";
            }, j2 = p2 ? 3 : 1; j2 > 0 && "break" !== P2(j2); j2--)
              ;
          e2.placement !== C2 && (e2.modifiersData[n2]._skip = true, e2.placement = C2, e2.reset = true);
        }
      }, requiresIfExists: ["offset"], data: { _skip: false } };
      function Ve(t2, e2, i2) {
        return void 0 === i2 && (i2 = { x: 0, y: 0 }), { top: t2.top - e2.height - i2.y, right: t2.right - e2.width + i2.x, bottom: t2.bottom - e2.height + i2.y, left: t2.left - e2.width - i2.x };
      }
      function Ue(t2) {
        return [xt, Dt, Lt, St].some(function(e2) {
          return t2[e2] >= 0;
        });
      }
      const Ke = { name: "hide", enabled: true, phase: "main", requiresIfExists: ["preventOverflow"], fn: function(t2) {
        var e2 = t2.state, i2 = t2.name, n2 = e2.rects.reference, s2 = e2.rects.popper, o2 = e2.modifiersData.preventOverflow, r2 = ze(e2, { elementContext: "reference" }), a2 = ze(e2, { altBoundary: true }), l2 = Ve(r2, n2), c2 = Ve(a2, s2, o2), h2 = Ue(l2), u2 = Ue(c2);
        e2.modifiersData[i2] = { referenceClippingOffsets: l2, popperEscapeOffsets: c2, isReferenceHidden: h2, hasPopperEscaped: u2 }, e2.attributes.popper = Object.assign({}, e2.attributes.popper, { "data-popper-reference-hidden": h2, "data-popper-escaped": u2 });
      } }, Qe = { name: "offset", enabled: true, phase: "main", requires: ["popperOffsets"], fn: function(t2) {
        var e2 = t2.state, i2 = t2.options, n2 = t2.name, s2 = i2.offset, o2 = void 0 === s2 ? [0, 0] : s2, r2 = Wt.reduce(function(t3, i3) {
          return t3[i3] = function(t4, e3, i4) {
            var n3 = se(t4), s3 = [St, xt].indexOf(n3) >= 0 ? -1 : 1, o3 = "function" == typeof i4 ? i4(Object.assign({}, e3, { placement: t4 })) : i4, r3 = o3[0], a3 = o3[1];
            return r3 = r3 || 0, a3 = (a3 || 0) * s3, [St, Dt].indexOf(n3) >= 0 ? { x: a3, y: r3 } : { x: r3, y: a3 };
          }(i3, e2.rects, o2), t3;
        }, {}), a2 = r2[e2.placement], l2 = a2.x, c2 = a2.y;
        null != e2.modifiersData.popperOffsets && (e2.modifiersData.popperOffsets.x += l2, e2.modifiersData.popperOffsets.y += c2), e2.modifiersData[n2] = r2;
      } }, Ye = { name: "popperOffsets", enabled: true, phase: "read", fn: function(t2) {
        var e2 = t2.state, i2 = t2.name;
        e2.modifiersData[i2] = We({ reference: e2.rects.reference, element: e2.rects.popper, strategy: "absolute", placement: e2.placement });
      }, data: {} }, Xe = { name: "preventOverflow", enabled: true, phase: "main", fn: function(t2) {
        var e2 = t2.state, i2 = t2.options, n2 = t2.name, s2 = i2.mainAxis, o2 = void 0 === s2 || s2, r2 = i2.altAxis, a2 = void 0 !== r2 && r2, l2 = i2.boundary, c2 = i2.rootBoundary, h2 = i2.altBoundary, u2 = i2.padding, d2 = i2.tether, f2 = void 0 === d2 || d2, p2 = i2.tetherOffset, g2 = void 0 === p2 ? 0 : p2, m2 = ze(e2, { boundary: l2, rootBoundary: c2, padding: u2, altBoundary: h2 }), _2 = se(e2.placement), b2 = Ce(e2.placement), v2 = !b2, y2 = ve(_2), w2 = "x" === y2 ? "y" : "x", A2 = e2.modifiersData.popperOffsets, E2 = e2.rects.reference, C2 = e2.rects.popper, T2 = "function" == typeof g2 ? g2(Object.assign({}, e2.rects, { placement: e2.placement })) : g2, O2 = "number" == typeof T2 ? { mainAxis: T2, altAxis: T2 } : Object.assign({ mainAxis: 0, altAxis: 0 }, T2), k2 = e2.modifiersData.offset ? e2.modifiersData.offset[e2.placement] : null, x2 = { x: 0, y: 0 };
        if (A2) {
          if (o2) {
            var L2, D2 = "y" === y2 ? xt : St, S2 = "y" === y2 ? Lt : Dt, N2 = "y" === y2 ? "height" : "width", I2 = A2[y2], M2 = I2 + m2[D2], P2 = I2 - m2[S2], j2 = f2 ? -C2[N2] / 2 : 0, $2 = b2 === Mt ? E2[N2] : C2[N2], F2 = b2 === Mt ? -C2[N2] : -E2[N2], H2 = e2.elements.arrow, B2 = f2 && H2 ? ue(H2) : { width: 0, height: 0 }, W2 = e2.modifiersData["arrow#persistent"] ? e2.modifiersData["arrow#persistent"].padding : { top: 0, right: 0, bottom: 0, left: 0 }, z2 = W2[D2], q2 = W2[S2], R2 = ye(0, E2[N2], B2[N2]), V2 = v2 ? E2[N2] / 2 - j2 - R2 - z2 - O2.mainAxis : $2 - R2 - z2 - O2.mainAxis, U2 = v2 ? -E2[N2] / 2 + j2 + R2 + q2 + O2.mainAxis : F2 + R2 + q2 + O2.mainAxis, K2 = e2.elements.arrow && be(e2.elements.arrow), Q2 = K2 ? "y" === y2 ? K2.clientTop || 0 : K2.clientLeft || 0 : 0, Y2 = null != (L2 = null == k2 ? void 0 : k2[y2]) ? L2 : 0, X2 = I2 + U2 - Y2, G2 = ye(f2 ? re(M2, I2 + V2 - Y2 - Q2) : M2, I2, f2 ? oe(P2, X2) : P2);
            A2[y2] = G2, x2[y2] = G2 - I2;
          }
          if (a2) {
            var J2, Z2 = "x" === y2 ? xt : St, tt2 = "x" === y2 ? Lt : Dt, et2 = A2[w2], it2 = "y" === w2 ? "height" : "width", nt2 = et2 + m2[Z2], st2 = et2 - m2[tt2], ot2 = -1 !== [xt, St].indexOf(_2), rt2 = null != (J2 = null == k2 ? void 0 : k2[w2]) ? J2 : 0, at2 = ot2 ? nt2 : et2 - E2[it2] - C2[it2] - rt2 + O2.altAxis, lt2 = ot2 ? et2 + E2[it2] + C2[it2] - rt2 - O2.altAxis : st2, ct2 = f2 && ot2 ? function(t3, e3, i3) {
              var n3 = ye(t3, e3, i3);
              return n3 > i3 ? i3 : n3;
            }(at2, et2, lt2) : ye(f2 ? at2 : nt2, et2, f2 ? lt2 : st2);
            A2[w2] = ct2, x2[w2] = ct2 - et2;
          }
          e2.modifiersData[n2] = x2;
        }
      }, requiresIfExists: ["offset"] };
      function Ge(t2, e2, i2) {
        void 0 === i2 && (i2 = false);
        var n2, s2, o2 = ee(e2), r2 = ee(e2) && function(t3) {
          var e3 = t3.getBoundingClientRect(), i3 = ae(e3.width) / t3.offsetWidth || 1, n3 = ae(e3.height) / t3.offsetHeight || 1;
          return 1 !== i3 || 1 !== n3;
        }(e2), a2 = ge(e2), l2 = he(t2, r2, i2), c2 = { scrollLeft: 0, scrollTop: 0 }, h2 = { x: 0, y: 0 };
        return (o2 || !o2 && !i2) && (("body" !== Jt(e2) || je(a2)) && (c2 = (n2 = e2) !== Zt(n2) && ee(n2) ? { scrollLeft: (s2 = n2).scrollLeft, scrollTop: s2.scrollTop } : Me(n2)), ee(e2) ? ((h2 = he(e2, true)).x += e2.clientLeft, h2.y += e2.clientTop) : a2 && (h2.x = Pe(a2))), { x: l2.left + c2.scrollLeft - h2.x, y: l2.top + c2.scrollTop - h2.y, width: l2.width, height: l2.height };
      }
      function Je(t2) {
        var e2 = /* @__PURE__ */ new Map(), i2 = /* @__PURE__ */ new Set(), n2 = [];
        function s2(t3) {
          i2.add(t3.name), [].concat(t3.requires || [], t3.requiresIfExists || []).forEach(function(t4) {
            if (!i2.has(t4)) {
              var n3 = e2.get(t4);
              n3 && s2(n3);
            }
          }), n2.push(t3);
        }
        return t2.forEach(function(t3) {
          e2.set(t3.name, t3);
        }), t2.forEach(function(t3) {
          i2.has(t3.name) || s2(t3);
        }), n2;
      }
      var Ze = { placement: "bottom", modifiers: [], strategy: "absolute" };
      function ti() {
        for (var t2 = arguments.length, e2 = new Array(t2), i2 = 0; i2 < t2; i2++)
          e2[i2] = arguments[i2];
        return !e2.some(function(t3) {
          return !(t3 && "function" == typeof t3.getBoundingClientRect);
        });
      }
      function ei(t2) {
        void 0 === t2 && (t2 = {});
        var e2 = t2, i2 = e2.defaultModifiers, n2 = void 0 === i2 ? [] : i2, s2 = e2.defaultOptions, o2 = void 0 === s2 ? Ze : s2;
        return function(t3, e3, i3) {
          void 0 === i3 && (i3 = o2);
          var s3, r2, a2 = { placement: "bottom", orderedModifiers: [], options: Object.assign({}, Ze, o2), modifiersData: {}, elements: { reference: t3, popper: e3 }, attributes: {}, styles: {} }, l2 = [], c2 = false, h2 = { state: a2, setOptions: function(i4) {
            var s4 = "function" == typeof i4 ? i4(a2.options) : i4;
            u2(), a2.options = Object.assign({}, o2, a2.options, s4), a2.scrollParents = { reference: te(t3) ? Fe(t3) : t3.contextElement ? Fe(t3.contextElement) : [], popper: Fe(e3) };
            var r3, c3, d2 = function(t4) {
              var e4 = Je(t4);
              return Gt.reduce(function(t5, i5) {
                return t5.concat(e4.filter(function(t6) {
                  return t6.phase === i5;
                }));
              }, []);
            }((r3 = [].concat(n2, a2.options.modifiers), c3 = r3.reduce(function(t4, e4) {
              var i5 = t4[e4.name];
              return t4[e4.name] = i5 ? Object.assign({}, i5, e4, { options: Object.assign({}, i5.options, e4.options), data: Object.assign({}, i5.data, e4.data) }) : e4, t4;
            }, {}), Object.keys(c3).map(function(t4) {
              return c3[t4];
            })));
            return a2.orderedModifiers = d2.filter(function(t4) {
              return t4.enabled;
            }), a2.orderedModifiers.forEach(function(t4) {
              var e4 = t4.name, i5 = t4.options, n3 = void 0 === i5 ? {} : i5, s5 = t4.effect;
              if ("function" == typeof s5) {
                var o3 = s5({ state: a2, name: e4, instance: h2, options: n3 });
                l2.push(o3 || function() {
                });
              }
            }), h2.update();
          }, forceUpdate: function() {
            if (!c2) {
              var t4 = a2.elements, e4 = t4.reference, i4 = t4.popper;
              if (ti(e4, i4)) {
                a2.rects = { reference: Ge(e4, be(i4), "fixed" === a2.options.strategy), popper: ue(i4) }, a2.reset = false, a2.placement = a2.options.placement, a2.orderedModifiers.forEach(function(t5) {
                  return a2.modifiersData[t5.name] = Object.assign({}, t5.data);
                });
                for (var n3 = 0; n3 < a2.orderedModifiers.length; n3++)
                  if (true !== a2.reset) {
                    var s4 = a2.orderedModifiers[n3], o3 = s4.fn, r3 = s4.options, l3 = void 0 === r3 ? {} : r3, u3 = s4.name;
                    "function" == typeof o3 && (a2 = o3({ state: a2, options: l3, name: u3, instance: h2 }) || a2);
                  } else
                    a2.reset = false, n3 = -1;
              }
            }
          }, update: (s3 = function() {
            return new Promise(function(t4) {
              h2.forceUpdate(), t4(a2);
            });
          }, function() {
            return r2 || (r2 = new Promise(function(t4) {
              Promise.resolve().then(function() {
                r2 = void 0, t4(s3());
              });
            })), r2;
          }), destroy: function() {
            u2(), c2 = true;
          } };
          if (!ti(t3, e3))
            return h2;
          function u2() {
            l2.forEach(function(t4) {
              return t4();
            }), l2 = [];
          }
          return h2.setOptions(i3).then(function(t4) {
            !c2 && i3.onFirstUpdate && i3.onFirstUpdate(t4);
          }), h2;
        };
      }
      var ii = ei(), ni = ei({ defaultModifiers: [Le, Ye, ke, ne] }), si = ei({ defaultModifiers: [Le, Ye, ke, ne, Qe, Re, Xe, Ee, Ke] });
      const oi = Object.freeze(Object.defineProperty({ __proto__: null, popperGenerator: ei, detectOverflow: ze, createPopperBase: ii, createPopper: si, createPopperLite: ni, top: xt, bottom: Lt, right: Dt, left: St, auto: Nt, basePlacements: It, start: Mt, end: Pt, clippingParents: jt, viewport: $t, popper: Ft, reference: Ht, variationPlacements: Bt, placements: Wt, beforeRead: zt, read: qt, afterRead: Rt, beforeMain: Vt, main: Ut, afterMain: Kt, beforeWrite: Qt, write: Yt, afterWrite: Xt, modifierPhases: Gt, applyStyles: ne, arrow: Ee, computeStyles: ke, eventListeners: Le, flip: Re, hide: Ke, offset: Qe, popperOffsets: Ye, preventOverflow: Xe }, Symbol.toStringTag, { value: "Module" })), ri = "dropdown", ai = ".coreui.dropdown", li = "ArrowUp", ci = "ArrowDown", hi = `hide${ai}`, ui = `hidden${ai}`, di = `show${ai}`, fi = `shown${ai}`, pi = `click${ai}.data-api`, gi = `keydown${ai}.data-api`, mi = `keyup${ai}.data-api`, _i = "show", bi = '[data-coreui-toggle="dropdown"]:not(.disabled):not(:disabled)', vi = `${bi}.show`, yi = ".dropdown-menu", wi = d() ? "top-end" : "top-start", Ai = d() ? "top-start" : "top-end", Ei = d() ? "bottom-end" : "bottom-start", Ci = d() ? "bottom-start" : "bottom-end", Ti = d() ? "left-start" : "right-start", Oi = d() ? "right-start" : "left-start", ki = { autoClose: true, boundary: "clippingParents", display: "dynamic", offset: [0, 2], popperConfig: null, reference: "toggle" }, xi = { autoClose: "(boolean|string)", boundary: "(string|element)", display: "string", offset: "(array|string|function)", popperConfig: "(null|object|function)", reference: "(string|element|object)" };
      class Li extends B {
        constructor(t2, e2) {
          super(t2, e2), this._popper = null, this._parent = this._element.parentNode, this._menu = z.next(this._element, yi)[0] || z.prev(this._element, yi)[0] || z.findOne(yi, this._parent), this._inNavbar = this._detectNavbar();
        }
        static get Default() {
          return ki;
        }
        static get DefaultType() {
          return xi;
        }
        static get NAME() {
          return ri;
        }
        toggle() {
          return this._isShown() ? this.hide() : this.show();
        }
        show() {
          if (r(this._element) || this._isShown())
            return;
          const t2 = { relatedTarget: this._element };
          if (!N.trigger(this._element, di, t2).defaultPrevented) {
            if (this._createPopper(), "ontouchstart" in document.documentElement && !this._parent.closest(".navbar-nav"))
              for (const t3 of [].concat(...document.body.children))
                N.on(t3, "mouseover", l);
            this._element.focus(), this._element.setAttribute("aria-expanded", true), this._menu.classList.add(_i), this._element.classList.add(_i), N.trigger(this._element, fi, t2);
          }
        }
        hide() {
          if (r(this._element) || !this._isShown())
            return;
          const t2 = { relatedTarget: this._element };
          this._completeHide(t2);
        }
        dispose() {
          this._popper && this._popper.destroy(), super.dispose();
        }
        update() {
          this._inNavbar = this._detectNavbar(), this._popper && this._popper.update();
        }
        _completeHide(t2) {
          if (!N.trigger(this._element, hi, t2).defaultPrevented) {
            if ("ontouchstart" in document.documentElement)
              for (const t3 of [].concat(...document.body.children))
                N.off(t3, "mouseover", l);
            this._popper && this._popper.destroy(), this._menu.classList.remove(_i), this._element.classList.remove(_i), this._element.setAttribute("aria-expanded", "false"), F.removeDataAttribute(this._menu, "popper"), N.trigger(this._element, ui, t2);
          }
        }
        _getConfig(t2) {
          if ("object" == typeof (t2 = super._getConfig(t2)).reference && !n(t2.reference) && "function" != typeof t2.reference.getBoundingClientRect)
            throw new TypeError(`${ri.toUpperCase()}: Option "reference" provided type "object" without a required "getBoundingClientRect" method.`);
          return t2;
        }
        _createPopper() {
          if (void 0 === oi)
            throw new TypeError("Bootstrap's dropdowns require Popper (https://popper.js.org)");
          let t2 = this._element;
          "parent" === this._config.reference ? t2 = this._parent : n(this._config.reference) ? t2 = s(this._config.reference) : "object" == typeof this._config.reference && (t2 = this._config.reference);
          const e2 = this._getPopperConfig();
          this._popper = si(t2, this._menu, e2);
        }
        _isShown() {
          return this._menu.classList.contains(_i);
        }
        _getPlacement() {
          const t2 = this._parent;
          if (t2.classList.contains("dropend"))
            return Ti;
          if (t2.classList.contains("dropstart"))
            return Oi;
          if (t2.classList.contains("dropup-center"))
            return "top";
          if (t2.classList.contains("dropdown-center"))
            return "bottom";
          const e2 = "end" === getComputedStyle(this._menu).getPropertyValue("--cui-position").trim();
          return t2.classList.contains("dropup") ? e2 ? Ai : wi : e2 ? Ci : Ei;
        }
        _detectNavbar() {
          return null !== this._element.closest(".navbar");
        }
        _getOffset() {
          const { offset: t2 } = this._config;
          return "string" == typeof t2 ? t2.split(",").map((t3) => Number.parseInt(t3, 10)) : "function" == typeof t2 ? (e2) => t2(e2, this._element) : t2;
        }
        _getPopperConfig() {
          const t2 = { placement: this._getPlacement(), modifiers: [{ name: "preventOverflow", options: { boundary: this._config.boundary } }, { name: "offset", options: { offset: this._getOffset() } }] };
          return (this._inNavbar || "static" === this._config.display) && (F.setDataAttribute(this._menu, "popper", "static"), t2.modifiers = [{ name: "applyStyles", enabled: false }]), { ...t2, ...p(this._config.popperConfig, [t2]) };
        }
        _selectMenuItem({ key: t2, target: e2 }) {
          const i2 = z.find(".dropdown-menu .dropdown-item:not(.disabled):not(:disabled)", this._menu).filter((t3) => o(t3));
          i2.length && m(i2, e2, t2 === ci, !i2.includes(e2)).focus();
        }
        static jQueryInterface(t2) {
          return this.each(function() {
            const e2 = Li.getOrCreateInstance(this, t2);
            if ("string" == typeof t2) {
              if (void 0 === e2[t2])
                throw new TypeError(`No method named "${t2}"`);
              e2[t2]();
            }
          });
        }
        static clearMenus(t2) {
          if (2 === t2.button || "keyup" === t2.type && "Tab" !== t2.key)
            return;
          const e2 = z.find(vi);
          for (const i2 of e2) {
            const e3 = Li.getInstance(i2);
            if (!e3 || false === e3._config.autoClose)
              continue;
            const n2 = t2.composedPath(), s2 = n2.includes(e3._menu);
            if (n2.includes(e3._element) || "inside" === e3._config.autoClose && !s2 || "outside" === e3._config.autoClose && s2)
              continue;
            if (e3._menu.contains(t2.target) && ("keyup" === t2.type && "Tab" === t2.key || /input|select|option|textarea|form/i.test(t2.target.tagName)))
              continue;
            const o2 = { relatedTarget: e3._element };
            "click" === t2.type && (o2.clickEvent = t2), e3._completeHide(o2);
          }
        }
        static dataApiKeydownHandler(t2) {
          const e2 = /input|textarea/i.test(t2.target.tagName), i2 = "Escape" === t2.key, n2 = [li, ci].includes(t2.key);
          if (!n2 && !i2)
            return;
          if (e2 && !i2)
            return;
          t2.preventDefault();
          const s2 = this.matches(bi) ? this : z.prev(this, bi)[0] || z.next(this, bi)[0] || z.findOne(bi, t2.delegateTarget.parentNode), o2 = Li.getOrCreateInstance(s2);
          if (n2)
            return t2.stopPropagation(), o2.show(), void o2._selectMenuItem(t2);
          o2._isShown() && (t2.stopPropagation(), o2.hide(), s2.focus());
        }
      }
      N.on(document, gi, bi, Li.dataApiKeydownHandler), N.on(document, gi, yi, Li.dataApiKeydownHandler), N.on(document, pi, Li.clearMenus), N.on(document, mi, Li.clearMenus), N.on(document, pi, bi, function(t2) {
        t2.preventDefault(), Li.getOrCreateInstance(this).toggle();
      }), f(Li);
      const Di = ".fixed-top, .fixed-bottom, .is-fixed, .sticky-top", Si = ".sticky-top", Ni = "padding-right", Ii = "margin-right";
      class Mi {
        constructor() {
          this._element = document.body;
        }
        getWidth() {
          const t2 = document.documentElement.clientWidth;
          return Math.abs(window.innerWidth - t2);
        }
        hide() {
          const t2 = this.getWidth();
          this._disableOverFlow(), this._setElementAttributes(this._element, Ni, (e2) => e2 + t2), this._setElementAttributes(Di, Ni, (e2) => e2 + t2), this._setElementAttributes(Si, Ii, (e2) => e2 - t2);
        }
        reset() {
          this._resetElementAttributes(this._element, "overflow"), this._resetElementAttributes(this._element, Ni), this._resetElementAttributes(Di, Ni), this._resetElementAttributes(Si, Ii);
        }
        isOverflowing() {
          return this.getWidth() > 0;
        }
        _disableOverFlow() {
          this._saveInitialAttribute(this._element, "overflow"), this._element.style.overflow = "hidden";
        }
        _setElementAttributes(t2, e2, i2) {
          const n2 = this.getWidth();
          this._applyManipulationCallback(t2, (t3) => {
            if (t3 !== this._element && window.innerWidth > t3.clientWidth + n2)
              return;
            this._saveInitialAttribute(t3, e2);
            const s2 = window.getComputedStyle(t3).getPropertyValue(e2);
            t3.style.setProperty(e2, `${i2(Number.parseFloat(s2))}px`);
          });
        }
        _saveInitialAttribute(t2, e2) {
          const i2 = t2.style.getPropertyValue(e2);
          i2 && F.setDataAttribute(t2, e2, i2);
        }
        _resetElementAttributes(t2, e2) {
          this._applyManipulationCallback(t2, (t3) => {
            const i2 = F.getDataAttribute(t3, e2);
            null !== i2 ? (F.removeDataAttribute(t3, e2), t3.style.setProperty(e2, i2)) : t3.style.removeProperty(e2);
          });
        }
        _applyManipulationCallback(t2, e2) {
          if (n(t2))
            e2(t2);
          else
            for (const i2 of z.find(t2, this._element))
              e2(i2);
        }
      }
      const Pi = "show", ji = "mousedown.coreui.backdrop", $i = { className: "modal-backdrop", clickCallback: null, isAnimated: false, isVisible: true, rootElement: "body" }, Fi = { className: "string", clickCallback: "(function|null)", isAnimated: "boolean", isVisible: "boolean", rootElement: "(element|string)" };
      class Hi extends H {
        constructor(t2) {
          super(), this._config = this._getConfig(t2), this._isAppended = false, this._element = null;
        }
        static get Default() {
          return $i;
        }
        static get DefaultType() {
          return Fi;
        }
        static get NAME() {
          return "backdrop";
        }
        show(t2) {
          if (!this._config.isVisible)
            return void p(t2);
          this._append();
          const e2 = this._getElement();
          this._config.isAnimated && c(e2), e2.classList.add(Pi), this._emulateAnimation(() => {
            p(t2);
          });
        }
        hide(t2) {
          this._config.isVisible ? (this._getElement().classList.remove(Pi), this._emulateAnimation(() => {
            this.dispose(), p(t2);
          })) : p(t2);
        }
        dispose() {
          this._isAppended && (N.off(this._element, ji), this._element.remove(), this._isAppended = false);
        }
        _getElement() {
          if (!this._element) {
            const t2 = document.createElement("div");
            t2.className = this._config.className, this._config.isAnimated && t2.classList.add("fade"), this._element = t2;
          }
          return this._element;
        }
        _configAfterMerge(t2) {
          return t2.rootElement = s(t2.rootElement), t2;
        }
        _append() {
          if (this._isAppended)
            return;
          const t2 = this._getElement();
          this._config.rootElement.append(t2), N.on(t2, ji, () => {
            p(this._config.clickCallback);
          }), this._isAppended = true;
        }
        _emulateAnimation(t2) {
          g(t2, this._getElement(), this._config.isAnimated);
        }
      }
      const Bi = ".coreui.focustrap", Wi = `focusin${Bi}`, zi = `keydown.tab${Bi}`, qi = "backward", Ri = { autofocus: true, trapElement: null }, Vi = { autofocus: "boolean", trapElement: "element" };
      class Ui extends H {
        constructor(t2) {
          super(), this._config = this._getConfig(t2), this._isActive = false, this._lastTabNavDirection = null;
        }
        static get Default() {
          return Ri;
        }
        static get DefaultType() {
          return Vi;
        }
        static get NAME() {
          return "focustrap";
        }
        activate() {
          this._isActive || (this._config.autofocus && this._config.trapElement.focus(), N.off(document, Bi), N.on(document, Wi, (t2) => this._handleFocusin(t2)), N.on(document, zi, (t2) => this._handleKeydown(t2)), this._isActive = true);
        }
        deactivate() {
          this._isActive && (this._isActive = false, N.off(document, Bi));
        }
        _handleFocusin(t2) {
          const { trapElement: e2 } = this._config;
          if (t2.target === document || t2.target === e2 || e2.contains(t2.target))
            return;
          const i2 = z.focusableChildren(e2);
          0 === i2.length ? e2.focus() : this._lastTabNavDirection === qi ? i2[i2.length - 1].focus() : i2[0].focus();
        }
        _handleKeydown(t2) {
          "Tab" === t2.key && (this._lastTabNavDirection = t2.shiftKey ? qi : "forward");
        }
      }
      const Ki = "hidden.coreui.modal", Qi = "show.coreui.modal", Yi = "modal-open", Xi = "show", Gi = "modal-static", Ji = { backdrop: true, focus: true, keyboard: true }, Zi = { backdrop: "(boolean|string)", focus: "boolean", keyboard: "boolean" };
      class tn extends B {
        constructor(t2, e2) {
          super(t2, e2), this._dialog = z.findOne(".modal-dialog", this._element), this._backdrop = this._initializeBackDrop(), this._focustrap = this._initializeFocusTrap(), this._isShown = false, this._isTransitioning = false, this._scrollBar = new Mi(), this._addEventListeners();
        }
        static get Default() {
          return Ji;
        }
        static get DefaultType() {
          return Zi;
        }
        static get NAME() {
          return "modal";
        }
        toggle(t2) {
          return this._isShown ? this.hide() : this.show(t2);
        }
        show(t2) {
          this._isShown || this._isTransitioning || N.trigger(this._element, Qi, { relatedTarget: t2 }).defaultPrevented || (this._isShown = true, this._isTransitioning = true, this._scrollBar.hide(), document.body.classList.add(Yi), this._adjustDialog(), this._backdrop.show(() => this._showElement(t2)));
        }
        hide() {
          this._isShown && !this._isTransitioning && (N.trigger(this._element, "hide.coreui.modal").defaultPrevented || (this._isShown = false, this._isTransitioning = true, this._focustrap.deactivate(), this._element.classList.remove(Xi), this._queueCallback(() => this._hideModal(), this._element, this._isAnimated())));
        }
        dispose() {
          for (const t2 of [window, this._dialog])
            N.off(t2, ".coreui.modal");
          this._backdrop.dispose(), this._focustrap.deactivate(), super.dispose();
        }
        handleUpdate() {
          this._adjustDialog();
        }
        _initializeBackDrop() {
          return new Hi({ isVisible: Boolean(this._config.backdrop), isAnimated: this._isAnimated() });
        }
        _initializeFocusTrap() {
          return new Ui({ trapElement: this._element });
        }
        _showElement(t2) {
          document.body.contains(this._element) || document.body.append(this._element), this._element.style.display = "block", this._element.removeAttribute("aria-hidden"), this._element.setAttribute("aria-modal", true), this._element.setAttribute("role", "dialog"), this._element.scrollTop = 0;
          const e2 = z.findOne(".modal-body", this._dialog);
          e2 && (e2.scrollTop = 0), c(this._element), this._element.classList.add(Xi), this._queueCallback(() => {
            this._config.focus && this._focustrap.activate(), this._isTransitioning = false, N.trigger(this._element, "shown.coreui.modal", { relatedTarget: t2 });
          }, this._dialog, this._isAnimated());
        }
        _addEventListeners() {
          N.on(this._element, "keydown.dismiss.coreui.modal", (t2) => {
            if ("Escape" === t2.key)
              return this._config.keyboard ? (t2.preventDefault(), void this.hide()) : void this._triggerBackdropTransition();
          }), N.on(window, "resize.coreui.modal", () => {
            this._isShown && !this._isTransitioning && this._adjustDialog();
          }), N.on(this._element, "mousedown.dismiss.coreui.modal", (t2) => {
            N.one(this._element, "click.dismiss.coreui.modal", (e2) => {
              this._element === t2.target && this._element === e2.target && ("static" !== this._config.backdrop ? this._config.backdrop && this.hide() : this._triggerBackdropTransition());
            });
          });
        }
        _hideModal() {
          this._element.style.display = "none", this._element.setAttribute("aria-hidden", true), this._element.removeAttribute("aria-modal"), this._element.removeAttribute("role"), this._isTransitioning = false, this._backdrop.hide(() => {
            document.body.classList.remove(Yi), this._resetAdjustments(), this._scrollBar.reset(), N.trigger(this._element, Ki);
          });
        }
        _isAnimated() {
          return this._element.classList.contains("fade");
        }
        _triggerBackdropTransition() {
          if (N.trigger(this._element, "hidePrevented.coreui.modal").defaultPrevented)
            return;
          const t2 = this._element.scrollHeight > document.documentElement.clientHeight, e2 = this._element.style.overflowY;
          "hidden" === e2 || this._element.classList.contains(Gi) || (t2 || (this._element.style.overflowY = "hidden"), this._element.classList.add(Gi), this._queueCallback(() => {
            this._element.classList.remove(Gi), this._queueCallback(() => {
              this._element.style.overflowY = e2;
            }, this._dialog);
          }, this._dialog), this._element.focus());
        }
        _adjustDialog() {
          const t2 = this._element.scrollHeight > document.documentElement.clientHeight, e2 = this._scrollBar.getWidth(), i2 = e2 > 0;
          if (i2 && !t2) {
            const t3 = d() ? "paddingLeft" : "paddingRight";
            this._element.style[t3] = `${e2}px`;
          }
          if (!i2 && t2) {
            const t3 = d() ? "paddingRight" : "paddingLeft";
            this._element.style[t3] = `${e2}px`;
          }
        }
        _resetAdjustments() {
          this._element.style.paddingLeft = "", this._element.style.paddingRight = "";
        }
        static jQueryInterface(t2, e2) {
          return this.each(function() {
            const i2 = tn.getOrCreateInstance(this, t2);
            if ("string" == typeof t2) {
              if (void 0 === i2[t2])
                throw new TypeError(`No method named "${t2}"`);
              i2[t2](e2);
            }
          });
        }
      }
      N.on(document, "click.coreui.modal.data-api", '[data-coreui-toggle="modal"]', function(t2) {
        const e2 = z.getElementFromSelector(this);
        ["A", "AREA"].includes(this.tagName) && t2.preventDefault(), N.one(e2, Qi, (t3) => {
          t3.defaultPrevented || N.one(e2, Ki, () => {
            o(this) && this.focus();
          });
        });
        const i2 = z.findOne(".modal.show");
        i2 && tn.getInstance(i2).hide(), tn.getOrCreateInstance(e2).toggle(this);
      }), q(tn), f(tn);
      const en = "coreui.navigation", nn = `.${en}`, sn = { activeLinksExact: true, groupsAutoCollapse: true }, on = { activeLinksExact: "boolean", groupsAutoCollapse: "(string|boolean)" }, rn = "active", an = "show", ln = "nav-group-toggle", cn = `click${nn}.data-api`, hn = `load${nn}.data-api`, un = ".nav-group", dn = ".nav-group-items", fn = ".nav-group-toggle";
      class pn extends B {
        constructor(t2, e2) {
          super(t2), this._config = this._getConfig(e2), this._setActiveLink(), this._addEventListeners(), P.set(t2, en, this);
        }
        static get Default() {
          return sn;
        }
        static get DATA_KEY() {
          return en;
        }
        static get DefaultType() {
          return on;
        }
        static get NAME() {
          return "navigation";
        }
        _getConfig(t2) {
          return { ...sn, ...F.getDataAttributes(this._element), ..."object" == typeof t2 ? t2 : {} };
        }
        _setActiveLink() {
          for (const t2 of Array.from(this._element.querySelectorAll(".nav-link"))) {
            if (t2.classList.contains(ln))
              continue;
            let e2 = String(window.location);
            const i2 = /\?./, n2 = /#./;
            (/\?.*=/.test(e2) || i2.test(e2)) && (e2 = e2.split("?")[0]), n2.test(e2) && (e2 = e2.split("#")[0]), this._config.activeLinksExact && t2.href === e2 && (t2.classList.add(rn), Array.from(this._getParents(t2, un)).forEach((t3) => {
              t3.classList.add(an), t3.setAttribute("aria-expanded", true);
            })), !this._config.activeLinksExact && t2.href.startsWith(e2) && (t2.classList.add(rn), Array.from(this._getParents(t2, un)).forEach((t3) => {
              t3.classList.add(an), t3.setAttribute("aria-expanded", true);
            }));
          }
        }
        _getParents(t2, e2) {
          const i2 = [];
          for (; t2 && t2 !== document; t2 = t2.parentNode)
            e2 ? t2.matches(e2) && i2.push(t2) : i2.push(t2);
          return i2;
        }
        _getAllSiblings(t2, e2) {
          const i2 = [];
          t2 = t2.parentNode.firstChild;
          do {
            3 !== t2.nodeType && 8 !== t2.nodeType && (e2 && !e2(t2) || i2.push(t2));
          } while (t2 = t2.nextSibling);
          return i2;
        }
        _getChildren(t2, e2) {
          const i2 = [];
          for (; t2; t2 = t2.nextSibling)
            1 === t2.nodeType && t2 !== e2 && i2.push(t2);
          return i2;
        }
        _getSiblings(t2, e2) {
          return this._getChildren(t2.parentNode.firstChild, t2).filter(e2);
        }
        _slideDown(t2) {
          t2.style.height = "auto";
          const e2 = t2.clientHeight;
          t2.style.height = "0px", setTimeout(() => {
            t2.style.height = `${e2}px`;
          }, 0), this._queueCallback(() => {
            t2.style.height = "auto";
          }, t2, true);
        }
        _slideUp(t2, e2) {
          const i2 = t2.clientHeight;
          t2.style.height = `${i2}px`, setTimeout(() => {
            t2.style.height = "0px";
          }, 0), this._queueCallback(() => {
            "function" == typeof e2 && e2();
          }, t2, true);
        }
        _toggleGroupItems(t2) {
          let e2 = t2.target;
          e2.classList.contains(ln) || (e2 = e2.closest(fn));
          const i2 = (t3) => Boolean(t3.classList.contains("nav-group") && t3.classList.contains(an));
          if (true === this._config.groupsAutoCollapse)
            for (const t3 of this._getSiblings(e2.parentNode, i2))
              this._slideUp(z.findOne(dn, t3), () => {
                t3.classList.remove(an), t3.setAttribute("aria-expanded", false);
              });
          e2.parentNode.classList.contains(an) ? this._slideUp(z.findOne(dn, e2.parentNode), () => {
            e2.parentNode.classList.remove(an), e2.parentNode.setAttribute("aria-expanded", false);
          }) : (e2.parentNode.classList.add(an), e2.parentNode.setAttribute("aria-expanded", true), this._slideDown(z.findOne(dn, e2.parentNode)));
        }
        _addEventListeners() {
          N.on(this._element, cn, fn, (t2) => {
            t2.preventDefault(), this._toggleGroupItems(t2, this);
          });
        }
        static navigationInterface(t2, e2) {
          const i2 = pn.getOrCreateInstance(t2, e2);
          if ("string" == typeof e2) {
            if (void 0 === i2[e2])
              throw new TypeError(`No method named "${e2}"`);
            i2[e2]();
          }
        }
        static jQueryInterface(t2) {
          return this.each(function() {
            pn.navigationInterface(this, t2);
          });
        }
      }
      N.on(window, hn, () => {
        for (const t2 of Array.from(document.querySelectorAll('[data-coreui="navigation"]')))
          pn.navigationInterface(t2);
      }), f(pn);
      const gn = ".coreui.offcanvas", mn = `load${gn}.data-api`, _n = "show", bn = "showing", vn = "hiding", yn = ".offcanvas.show", wn = `show${gn}`, An = `shown${gn}`, En = `hide${gn}`, Cn = `hidePrevented${gn}`, Tn = `hidden${gn}`, On = `resize${gn}`, kn = `click${gn}.data-api`, xn = `keydown.dismiss${gn}`, Ln = { backdrop: true, keyboard: true, scroll: false }, Dn = { backdrop: "(boolean|string)", keyboard: "boolean", scroll: "boolean" };
      class Sn extends B {
        constructor(t2, e2) {
          super(t2, e2), this._isShown = false, this._backdrop = this._initializeBackDrop(), this._focustrap = this._initializeFocusTrap(), this._addEventListeners();
        }
        static get Default() {
          return Ln;
        }
        static get DefaultType() {
          return Dn;
        }
        static get NAME() {
          return "offcanvas";
        }
        toggle(t2) {
          return this._isShown ? this.hide() : this.show(t2);
        }
        show(t2) {
          this._isShown || N.trigger(this._element, wn, { relatedTarget: t2 }).defaultPrevented || (this._isShown = true, this._backdrop.show(), this._config.scroll || new Mi().hide(), this._element.setAttribute("aria-modal", true), this._element.setAttribute("role", "dialog"), this._element.classList.add(bn), this._queueCallback(() => {
            this._config.scroll && !this._config.backdrop || this._focustrap.activate(), this._element.classList.add(_n), this._element.classList.remove(bn), N.trigger(this._element, An, { relatedTarget: t2 });
          }, this._element, true));
        }
        hide() {
          this._isShown && (N.trigger(this._element, En).defaultPrevented || (this._focustrap.deactivate(), this._element.blur(), this._isShown = false, this._element.classList.add(vn), this._backdrop.hide(), this._queueCallback(() => {
            this._element.classList.remove(_n, vn), this._element.removeAttribute("aria-modal"), this._element.removeAttribute("role"), this._config.scroll || new Mi().reset(), N.trigger(this._element, Tn);
          }, this._element, true)));
        }
        dispose() {
          this._backdrop.dispose(), this._focustrap.deactivate(), super.dispose();
        }
        _initializeBackDrop() {
          const t2 = Boolean(this._config.backdrop);
          return new Hi({ className: "offcanvas-backdrop", isVisible: t2, isAnimated: true, rootElement: this._element.parentNode, clickCallback: t2 ? () => {
            "static" !== this._config.backdrop ? this.hide() : N.trigger(this._element, Cn);
          } : null });
        }
        _initializeFocusTrap() {
          return new Ui({ trapElement: this._element });
        }
        _addEventListeners() {
          N.on(this._element, xn, (t2) => {
            "Escape" === t2.key && (this._config.keyboard ? this.hide() : N.trigger(this._element, Cn));
          });
        }
        static jQueryInterface(t2) {
          return this.each(function() {
            const e2 = Sn.getOrCreateInstance(this, t2);
            if ("string" == typeof t2) {
              if (void 0 === e2[t2] || t2.startsWith("_") || "constructor" === t2)
                throw new TypeError(`No method named "${t2}"`);
              e2[t2](this);
            }
          });
        }
      }
      N.on(document, kn, '[data-coreui-toggle="offcanvas"]', function(t2) {
        const e2 = z.getElementFromSelector(this);
        if (["A", "AREA"].includes(this.tagName) && t2.preventDefault(), r(this))
          return;
        N.one(e2, Tn, () => {
          o(this) && this.focus();
        });
        const i2 = z.findOne(yn);
        i2 && i2 !== e2 && Sn.getInstance(i2).hide(), Sn.getOrCreateInstance(e2).toggle(this);
      }), N.on(window, mn, () => {
        for (const t2 of z.find(yn))
          Sn.getOrCreateInstance(t2).show();
      }), N.on(window, On, () => {
        for (const t2 of z.find("[aria-modal][class*=show][class*=offcanvas-]"))
          "fixed" !== getComputedStyle(t2).position && Sn.getOrCreateInstance(t2).hide();
      }), q(Sn), f(Sn);
      const Nn = /* @__PURE__ */ new Set(["background", "cite", "href", "itemtype", "longdesc", "poster", "src", "xlink:href"]), In = /^(?:(?:https?|mailto|ftp|tel|file|sms):|[^#&/:?]*(?:[#/?]|$))/i, Mn = /^data:(?:image\/(?:bmp|gif|jpeg|jpg|png|tiff|webp)|video\/(?:mpeg|mp4|ogg|webm)|audio\/(?:mp3|oga|ogg|opus));base64,[\d+/a-z]+=*$/i, Pn = (t2, e2) => {
        const i2 = t2.nodeName.toLowerCase();
        return e2.includes(i2) ? !Nn.has(i2) || Boolean(In.test(t2.nodeValue) || Mn.test(t2.nodeValue)) : e2.filter((t3) => t3 instanceof RegExp).some((t3) => t3.test(i2));
      }, jn = { "*": ["class", "dir", "id", "lang", "role", /^aria-[\w-]*$/i], a: ["target", "href", "title", "rel"], area: [], b: [], br: [], col: [], code: [], div: [], em: [], hr: [], h1: [], h2: [], h3: [], h4: [], h5: [], h6: [], i: [], img: ["src", "srcset", "alt", "title", "width", "height"], li: [], ol: [], p: [], pre: [], s: [], small: [], span: [], sub: [], sup: [], strong: [], u: [], ul: [] }, $n = { allowList: jn, content: {}, extraClass: "", html: false, sanitize: true, sanitizeFn: null, template: "<div></div>" }, Fn = { allowList: "object", content: "object", extraClass: "(string|function)", html: "boolean", sanitize: "boolean", sanitizeFn: "(null|function)", template: "string" }, Hn = { entry: "(string|element|function|null)", selector: "(string|element)" };
      class Bn extends H {
        constructor(t2) {
          super(), this._config = this._getConfig(t2);
        }
        static get Default() {
          return $n;
        }
        static get DefaultType() {
          return Fn;
        }
        static get NAME() {
          return "TemplateFactory";
        }
        getContent() {
          return Object.values(this._config.content).map((t2) => this._resolvePossibleFunction(t2)).filter(Boolean);
        }
        hasContent() {
          return this.getContent().length > 0;
        }
        changeContent(t2) {
          return this._checkContent(t2), this._config.content = { ...this._config.content, ...t2 }, this;
        }
        toHtml() {
          const t2 = document.createElement("div");
          t2.innerHTML = this._maybeSanitize(this._config.template);
          for (const [e3, i3] of Object.entries(this._config.content))
            this._setContent(t2, i3, e3);
          const e2 = t2.children[0], i2 = this._resolvePossibleFunction(this._config.extraClass);
          return i2 && e2.classList.add(...i2.split(" ")), e2;
        }
        _typeCheckConfig(t2) {
          super._typeCheckConfig(t2), this._checkContent(t2.content);
        }
        _checkContent(t2) {
          for (const [e2, i2] of Object.entries(t2))
            super._typeCheckConfig({ selector: e2, entry: i2 }, Hn);
        }
        _setContent(t2, e2, i2) {
          const o2 = z.findOne(i2, t2);
          o2 && ((e2 = this._resolvePossibleFunction(e2)) ? n(e2) ? this._putElementInTemplate(s(e2), o2) : this._config.html ? o2.innerHTML = this._maybeSanitize(e2) : o2.textContent = e2 : o2.remove());
        }
        _maybeSanitize(t2) {
          return this._config.sanitize ? function(t3, e2, i2) {
            if (!t3.length)
              return t3;
            if (i2 && "function" == typeof i2)
              return i2(t3);
            const n2 = new window.DOMParser().parseFromString(t3, "text/html"), s2 = [].concat(...n2.body.querySelectorAll("*"));
            for (const t4 of s2) {
              const i3 = t4.nodeName.toLowerCase();
              if (!Object.keys(e2).includes(i3)) {
                t4.remove();
                continue;
              }
              const n3 = [].concat(...t4.attributes), s3 = [].concat(e2["*"] || [], e2[i3] || []);
              for (const e3 of n3)
                Pn(e3, s3) || t4.removeAttribute(e3.nodeName);
            }
            return n2.body.innerHTML;
          }(t2, this._config.allowList, this._config.sanitizeFn) : t2;
        }
        _resolvePossibleFunction(t2) {
          return p(t2, [this]);
        }
        _putElementInTemplate(t2, e2) {
          if (this._config.html)
            return e2.innerHTML = "", void e2.append(t2);
          e2.textContent = t2.textContent;
        }
      }
      const Wn = /* @__PURE__ */ new Set(["sanitize", "allowList", "sanitizeFn"]), zn = "fade", qn = "show", Rn = ".modal", Vn = "hide.coreui.modal", Un = "hover", Kn = "focus", Qn = { AUTO: "auto", TOP: "top", RIGHT: d() ? "left" : "right", BOTTOM: "bottom", LEFT: d() ? "right" : "left" }, Yn = { allowList: jn, animation: true, boundary: "clippingParents", container: false, customClass: "", delay: 0, fallbackPlacements: ["top", "right", "bottom", "left"], html: false, offset: [0, 0], placement: "top", popperConfig: null, sanitize: true, sanitizeFn: null, selector: false, template: '<div class="tooltip" role="tooltip"><div class="tooltip-arrow"></div><div class="tooltip-inner"></div></div>', title: "", trigger: "hover focus" }, Xn = { allowList: "object", animation: "boolean", boundary: "(string|element)", container: "(string|element|boolean)", customClass: "(string|function)", delay: "(number|object)", fallbackPlacements: "array", html: "boolean", offset: "(array|string|function)", placement: "(string|function)", popperConfig: "(null|object|function)", sanitize: "boolean", sanitizeFn: "(null|function)", selector: "(string|boolean)", template: "string", title: "(string|element|function)", trigger: "string" };
      class Gn extends B {
        constructor(t2, e2) {
          if (void 0 === oi)
            throw new TypeError("Bootstrap's tooltips require Popper (https://popper.js.org)");
          super(t2, e2), this._isEnabled = true, this._timeout = 0, this._isHovered = null, this._activeTrigger = {}, this._popper = null, this._templateFactory = null, this._newContent = null, this.tip = null, this._setListeners(), this._config.selector || this._fixTitle();
        }
        static get Default() {
          return Yn;
        }
        static get DefaultType() {
          return Xn;
        }
        static get NAME() {
          return "tooltip";
        }
        enable() {
          this._isEnabled = true;
        }
        disable() {
          this._isEnabled = false;
        }
        toggleEnabled() {
          this._isEnabled = !this._isEnabled;
        }
        toggle() {
          this._isEnabled && (this._activeTrigger.click = !this._activeTrigger.click, this._isShown() ? this._leave() : this._enter());
        }
        dispose() {
          clearTimeout(this._timeout), N.off(this._element.closest(Rn), Vn, this._hideModalHandler), this._element.getAttribute("data-coreui-original-title") && this._element.setAttribute("title", this._element.getAttribute("data-coreui-original-title")), this._disposePopper(), super.dispose();
        }
        show() {
          if ("none" === this._element.style.display)
            throw new Error("Please use show on visible elements");
          if (!this._isWithContent() || !this._isEnabled)
            return;
          const t2 = N.trigger(this._element, this.constructor.eventName("show")), e2 = (a(this._element) || this._element.ownerDocument.documentElement).contains(this._element);
          if (t2.defaultPrevented || !e2)
            return;
          this._disposePopper();
          const i2 = this._getTipElement();
          this._element.setAttribute("aria-describedby", i2.getAttribute("id"));
          const { container: n2 } = this._config;
          if (this._element.ownerDocument.documentElement.contains(this.tip) || (n2.append(i2), N.trigger(this._element, this.constructor.eventName("inserted"))), this._popper = this._createPopper(i2), i2.classList.add(qn), "ontouchstart" in document.documentElement)
            for (const t3 of [].concat(...document.body.children))
              N.on(t3, "mouseover", l);
          this._queueCallback(() => {
            N.trigger(this._element, this.constructor.eventName("shown")), false === this._isHovered && this._leave(), this._isHovered = false;
          }, this.tip, this._isAnimated());
        }
        hide() {
          if (this._isShown() && !N.trigger(this._element, this.constructor.eventName("hide")).defaultPrevented) {
            if (this._getTipElement().classList.remove(qn), "ontouchstart" in document.documentElement)
              for (const t2 of [].concat(...document.body.children))
                N.off(t2, "mouseover", l);
            this._activeTrigger.click = false, this._activeTrigger.focus = false, this._activeTrigger.hover = false, this._isHovered = null, this._queueCallback(() => {
              this._isWithActiveTrigger() || (this._isHovered || this._disposePopper(), this._element.removeAttribute("aria-describedby"), N.trigger(this._element, this.constructor.eventName("hidden")));
            }, this.tip, this._isAnimated());
          }
        }
        update() {
          this._popper && this._popper.update();
        }
        _isWithContent() {
          return Boolean(this._getTitle());
        }
        _getTipElement() {
          return this.tip || (this.tip = this._createTipElement(this._newContent || this._getContentForTemplate())), this.tip;
        }
        _createTipElement(t2) {
          const e2 = this._getTemplateFactory(t2).toHtml();
          if (!e2)
            return null;
          e2.classList.remove(zn, qn), e2.classList.add(`bs-${this.constructor.NAME}-auto`);
          const i2 = ((t3) => {
            do {
              t3 += Math.floor(1e6 * Math.random());
            } while (document.getElementById(t3));
            return t3;
          })(this.constructor.NAME).toString();
          return e2.setAttribute("id", i2), this._isAnimated() && e2.classList.add(zn), e2;
        }
        setContent(t2) {
          this._newContent = t2, this._isShown() && (this._disposePopper(), this.show());
        }
        _getTemplateFactory(t2) {
          return this._templateFactory ? this._templateFactory.changeContent(t2) : this._templateFactory = new Bn({ ...this._config, content: t2, extraClass: this._resolvePossibleFunction(this._config.customClass) }), this._templateFactory;
        }
        _getContentForTemplate() {
          return { ".tooltip-inner": this._getTitle() };
        }
        _getTitle() {
          return this._resolvePossibleFunction(this._config.title) || this._element.getAttribute("data-coreui-original-title");
        }
        _initializeOnDelegatedTarget(t2) {
          return this.constructor.getOrCreateInstance(t2.delegateTarget, this._getDelegateConfig());
        }
        _isAnimated() {
          return this._config.animation || this.tip && this.tip.classList.contains(zn);
        }
        _isShown() {
          return this.tip && this.tip.classList.contains(qn);
        }
        _createPopper(t2) {
          const e2 = p(this._config.placement, [this, t2, this._element]), i2 = Qn[e2.toUpperCase()];
          return si(this._element, t2, this._getPopperConfig(i2));
        }
        _getOffset() {
          const { offset: t2 } = this._config;
          return "string" == typeof t2 ? t2.split(",").map((t3) => Number.parseInt(t3, 10)) : "function" == typeof t2 ? (e2) => t2(e2, this._element) : t2;
        }
        _resolvePossibleFunction(t2) {
          return p(t2, [this._element]);
        }
        _getPopperConfig(t2) {
          const e2 = { placement: t2, modifiers: [{ name: "flip", options: { fallbackPlacements: this._config.fallbackPlacements } }, { name: "offset", options: { offset: this._getOffset() } }, { name: "preventOverflow", options: { boundary: this._config.boundary } }, { name: "arrow", options: { element: `.${this.constructor.NAME}-arrow` } }, { name: "preSetPlacement", enabled: true, phase: "beforeMain", fn: (t3) => {
            this._getTipElement().setAttribute("data-popper-placement", t3.state.placement);
          } }] };
          return { ...e2, ...p(this._config.popperConfig, [e2]) };
        }
        _setListeners() {
          const t2 = this._config.trigger.split(" ");
          for (const e2 of t2)
            if ("click" === e2)
              N.on(this._element, this.constructor.eventName("click"), this._config.selector, (t3) => {
                this._initializeOnDelegatedTarget(t3).toggle();
              });
            else if ("manual" !== e2) {
              const t3 = e2 === Un ? this.constructor.eventName("mouseenter") : this.constructor.eventName("focusin"), i2 = e2 === Un ? this.constructor.eventName("mouseleave") : this.constructor.eventName("focusout");
              N.on(this._element, t3, this._config.selector, (t4) => {
                const e3 = this._initializeOnDelegatedTarget(t4);
                e3._activeTrigger["focusin" === t4.type ? Kn : Un] = true, e3._enter();
              }), N.on(this._element, i2, this._config.selector, (t4) => {
                const e3 = this._initializeOnDelegatedTarget(t4);
                e3._activeTrigger["focusout" === t4.type ? Kn : Un] = e3._element.contains(t4.relatedTarget), e3._leave();
              });
            }
          this._hideModalHandler = () => {
            this._element && this.hide();
          }, N.on(this._element.closest(Rn), Vn, this._hideModalHandler);
        }
        _fixTitle() {
          const t2 = this._element.getAttribute("title");
          t2 && (this._element.getAttribute("aria-label") || this._element.textContent.trim() || this._element.setAttribute("aria-label", t2), this._element.setAttribute("data-coreui-original-title", t2), this._element.removeAttribute("title"));
        }
        _enter() {
          this._isShown() || this._isHovered ? this._isHovered = true : (this._isHovered = true, this._setTimeout(() => {
            this._isHovered && this.show();
          }, this._config.delay.show));
        }
        _leave() {
          this._isWithActiveTrigger() || (this._isHovered = false, this._setTimeout(() => {
            this._isHovered || this.hide();
          }, this._config.delay.hide));
        }
        _setTimeout(t2, e2) {
          clearTimeout(this._timeout), this._timeout = setTimeout(t2, e2);
        }
        _isWithActiveTrigger() {
          return Object.values(this._activeTrigger).includes(true);
        }
        _getConfig(t2) {
          const e2 = F.getDataAttributes(this._element);
          for (const t3 of Object.keys(e2))
            Wn.has(t3) && delete e2[t3];
          return t2 = { ...e2, ..."object" == typeof t2 && t2 ? t2 : {} }, t2 = this._mergeConfigObj(t2), t2 = this._configAfterMerge(t2), this._typeCheckConfig(t2), t2;
        }
        _configAfterMerge(t2) {
          return t2.container = false === t2.container ? document.body : s(t2.container), "number" == typeof t2.delay && (t2.delay = { show: t2.delay, hide: t2.delay }), "number" == typeof t2.title && (t2.title = t2.title.toString()), "number" == typeof t2.content && (t2.content = t2.content.toString()), t2;
        }
        _getDelegateConfig() {
          const t2 = {};
          for (const [e2, i2] of Object.entries(this._config))
            this.constructor.Default[e2] !== i2 && (t2[e2] = i2);
          return t2.selector = false, t2.trigger = "manual", t2;
        }
        _disposePopper() {
          this._popper && (this._popper.destroy(), this._popper = null), this.tip && (this.tip.remove(), this.tip = null);
        }
        static jQueryInterface(t2) {
          return this.each(function() {
            const e2 = Gn.getOrCreateInstance(this, t2);
            if ("string" == typeof t2) {
              if (void 0 === e2[t2])
                throw new TypeError(`No method named "${t2}"`);
              e2[t2]();
            }
          });
        }
      }
      f(Gn);
      const Jn = { ...Gn.Default, content: "", offset: [0, 8], placement: "right", template: '<div class="popover" role="tooltip"><div class="popover-arrow"></div><h3 class="popover-header"></h3><div class="popover-body"></div></div>', trigger: "click" }, Zn = { ...Gn.DefaultType, content: "(null|string|element|function)" };
      class ts extends Gn {
        static get Default() {
          return Jn;
        }
        static get DefaultType() {
          return Zn;
        }
        static get NAME() {
          return "popover";
        }
        _isWithContent() {
          return this._getTitle() || this._getContent();
        }
        _getContentForTemplate() {
          return { ".popover-header": this._getTitle(), ".popover-body": this._getContent() };
        }
        _getContent() {
          return this._resolvePossibleFunction(this._config.content);
        }
        static jQueryInterface(t2) {
          return this.each(function() {
            const e2 = ts.getOrCreateInstance(this, t2);
            if ("string" == typeof t2) {
              if (void 0 === e2[t2])
                throw new TypeError(`No method named "${t2}"`);
              e2[t2]();
            }
          });
        }
      }
      f(ts);
      const es = ".coreui.scrollspy", is = `activate${es}`, ns = `click${es}`, ss = `load${es}.data-api`, os = "active", rs = "[href]", as = { offset: null, rootMargin: "0px 0px -25%", smoothScroll: false, target: null, threshold: [0.1, 0.5, 1] }, ls = { offset: "(number|null)", rootMargin: "string", smoothScroll: "boolean", target: "element", threshold: "array" };
      class cs extends B {
        constructor(t2, e2) {
          super(t2, e2), this._targetLinks = /* @__PURE__ */ new Map(), this._observableSections = /* @__PURE__ */ new Map(), this._rootElement = "visible" === getComputedStyle(this._element).overflowY ? null : this._element, this._activeTarget = null, this._observer = null, this._previousScrollData = { visibleEntryTop: 0, parentScrollTop: 0 }, this.refresh();
        }
        static get Default() {
          return as;
        }
        static get DefaultType() {
          return ls;
        }
        static get NAME() {
          return "scrollspy";
        }
        refresh() {
          this._initializeTargetsAndObservables(), this._maybeEnableSmoothScroll(), this._observer ? this._observer.disconnect() : this._observer = this._getNewObserver();
          for (const t2 of this._observableSections.values())
            this._observer.observe(t2);
        }
        dispose() {
          this._observer.disconnect(), super.dispose();
        }
        _configAfterMerge(t2) {
          return t2.target = s(t2.target) || document.body, t2.rootMargin = t2.offset ? `${t2.offset}px 0px -30%` : t2.rootMargin, "string" == typeof t2.threshold && (t2.threshold = t2.threshold.split(",").map((t3) => Number.parseFloat(t3))), t2;
        }
        _maybeEnableSmoothScroll() {
          this._config.smoothScroll && (N.off(this._config.target, ns), N.on(this._config.target, ns, rs, (t2) => {
            const e2 = this._observableSections.get(t2.target.hash);
            if (e2) {
              t2.preventDefault();
              const i2 = this._rootElement || window, n2 = e2.offsetTop - this._element.offsetTop;
              if (i2.scrollTo)
                return void i2.scrollTo({ top: n2, behavior: "smooth" });
              i2.scrollTop = n2;
            }
          }));
        }
        _getNewObserver() {
          const t2 = { root: this._rootElement, threshold: this._config.threshold, rootMargin: this._config.rootMargin };
          return new IntersectionObserver((t3) => this._observerCallback(t3), t2);
        }
        _observerCallback(t2) {
          const e2 = (t3) => this._targetLinks.get(`#${t3.target.id}`), i2 = (t3) => {
            this._previousScrollData.visibleEntryTop = t3.target.offsetTop, this._process(e2(t3));
          }, n2 = (this._rootElement || document.documentElement).scrollTop, s2 = n2 >= this._previousScrollData.parentScrollTop;
          this._previousScrollData.parentScrollTop = n2;
          for (const o2 of t2) {
            if (!o2.isIntersecting) {
              this._activeTarget = null, this._clearActiveClass(e2(o2));
              continue;
            }
            const t3 = o2.target.offsetTop >= this._previousScrollData.visibleEntryTop;
            if (s2 && t3) {
              if (i2(o2), !n2)
                return;
            } else
              s2 || t3 || i2(o2);
          }
        }
        _initializeTargetsAndObservables() {
          this._targetLinks = /* @__PURE__ */ new Map(), this._observableSections = /* @__PURE__ */ new Map();
          const t2 = z.find(rs, this._config.target);
          for (const e2 of t2) {
            if (!e2.hash || r(e2))
              continue;
            const t3 = z.findOne(e2.hash, this._element);
            o(t3) && (this._targetLinks.set(e2.hash, e2), this._observableSections.set(e2.hash, t3));
          }
        }
        _process(t2) {
          this._activeTarget !== t2 && (this._clearActiveClass(this._config.target), this._activeTarget = t2, t2.classList.add(os), this._activateParents(t2), N.trigger(this._element, is, { relatedTarget: t2 }));
        }
        _activateParents(t2) {
          if (t2.classList.contains("dropdown-item"))
            z.findOne(".dropdown-toggle", t2.closest(".dropdown")).classList.add(os);
          else
            for (const e2 of z.parents(t2, ".nav, .list-group"))
              for (const t3 of z.prev(e2, ".nav-link, .nav-item > .nav-link, .list-group-item"))
                t3.classList.add(os);
        }
        _clearActiveClass(t2) {
          t2.classList.remove(os);
          const e2 = z.find("[href].active", t2);
          for (const t3 of e2)
            t3.classList.remove(os);
        }
        static jQueryInterface(t2) {
          return this.each(function() {
            const e2 = cs.getOrCreateInstance(this, t2);
            if ("string" == typeof t2) {
              if (void 0 === e2[t2] || t2.startsWith("_") || "constructor" === t2)
                throw new TypeError(`No method named "${t2}"`);
              e2[t2]();
            }
          });
        }
      }
      N.on(window, ss, () => {
        for (const t2 of z.find('[data-coreui-spy="scroll"]'))
          cs.getOrCreateInstance(t2);
      }), f(cs);
      const hs = ".coreui.sidebar", us = {}, ds = {}, fs = "hide", ps = "show", gs = "sidebar-narrow", ms = "sidebar-narrow-unfoldable", _s = `hide${hs}`, bs = `hidden${hs}`, vs = `show${hs}`, ys = `shown${hs}`, ws = `click${hs}.data-api`, As = `load${hs}.data-api`, Es = ".sidebar";
      class Cs extends B {
        constructor(t2, e2) {
          super(t2), this._config = this._getConfig(e2), this._show = this._isVisible(), this._mobile = this._isMobile(), this._overlaid = this._isOverlaid(), this._narrow = this._isNarrow(), this._unfoldable = this._isUnfoldable(), this._backdrop = this._initializeBackDrop(), this._addEventListeners();
        }
        static get Default() {
          return us;
        }
        static get DefaultType() {
          return ds;
        }
        static get NAME() {
          return "sidebar";
        }
        show() {
          N.trigger(this._element, vs), this._element.classList.contains(fs) && this._element.classList.remove(fs), this._isMobile() && (this._element.classList.add(ps), this._backdrop.show(), new Mi().hide()), this._queueCallback(() => {
            true === this._isVisible() && (this._show = true, (this._isMobile() || this._isOverlaid()) && this._addClickOutListener(), N.trigger(this._element, ys));
          }, this._element, true);
        }
        hide() {
          N.trigger(this._element, _s), this._element.classList.contains(ps) && this._element.classList.remove(ps), this._isMobile() ? (this._backdrop.hide(), new Mi().reset()) : this._element.classList.add(fs), this._queueCallback(() => {
            false === this._isVisible() && (this._show = false, (this._isMobile() || this._isOverlaid()) && this._removeClickOutListener(), N.trigger(this._element, bs));
          }, this._element, true);
        }
        toggle() {
          this._isVisible() ? this.hide() : this.show();
        }
        narrow() {
          this._isMobile() || (this._addClassName(gs), this._narrow = true);
        }
        unfoldable() {
          this._isMobile() || (this._addClassName(ms), this._unfoldable = true);
        }
        reset() {
          this._isMobile() || (this._narrow && (this._element.classList.remove(gs), this._narrow = false), this._unfoldable && (this._element.classList.remove(ms), this._unfoldable = false));
        }
        toggleNarrow() {
          this._narrow ? this.reset() : this.narrow();
        }
        toggleUnfoldable() {
          this._unfoldable ? this.reset() : this.unfoldable();
        }
        _getConfig(t2) {
          return { ...us, ...F.getDataAttributes(this._element), ..."object" == typeof t2 ? t2 : {} };
        }
        _initializeBackDrop() {
          return new Hi({ className: "sidebar-backdrop", isVisible: this._isMobile(), isAnimated: true, rootElement: this._element.parentNode, clickCallback: () => this.hide() });
        }
        _isMobile() {
          return Boolean(window.getComputedStyle(this._element, null).getPropertyValue("--cui-is-mobile"));
        }
        _isNarrow() {
          return this._element.classList.contains(gs);
        }
        _isOverlaid() {
          return this._element.classList.contains("sidebar-overlaid");
        }
        _isUnfoldable() {
          return this._element.classList.contains(ms);
        }
        _isVisible() {
          const t2 = this._element.getBoundingClientRect();
          return t2.top >= 0 && t2.left >= 0 && Math.floor(t2.bottom) <= (window.innerHeight || document.documentElement.clientHeight) && Math.floor(t2.right) <= (window.innerWidth || document.documentElement.clientWidth);
        }
        _addClassName(t2) {
          this._element.classList.add(t2);
        }
        _clickOutListener(t2, e2) {
          null === t2.target.closest(Es) && (t2.preventDefault(), t2.stopPropagation(), e2.hide());
        }
        _addClickOutListener() {
          N.on(document, ws, (t2) => {
            this._clickOutListener(t2, this);
          });
        }
        _removeClickOutListener() {
          N.off(document, ws);
        }
        _addEventListeners() {
          this._mobile && this._show && this._addClickOutListener(), this._overlaid && this._show && this._addClickOutListener(), N.on(this._element, ws, "[data-coreui-toggle]", (t2) => {
            t2.preventDefault();
            const e2 = F.getDataAttribute(t2.target, "toggle");
            "narrow" === e2 && this.toggleNarrow(), "unfoldable" === e2 && this.toggleUnfoldable();
          }), N.on(this._element, ws, '[data-coreui-close="sidebar"]', (t2) => {
            t2.preventDefault(), this.hide();
          }), N.on(window, "resize", () => {
            this._isMobile() && this._isVisible() && (this.hide(), this._backdrop = this._initializeBackDrop());
          });
        }
        static sidebarInterface(t2, e2) {
          const i2 = Cs.getOrCreateInstance(t2, e2);
          if ("string" == typeof e2) {
            if (void 0 === i2[e2])
              throw new TypeError(`No method named "${e2}"`);
            i2[e2]();
          }
        }
        static jQueryInterface(t2) {
          return this.each(function() {
            Cs.sidebarInterface(this, t2);
          });
        }
      }
      N.on(window, As, () => {
        for (const t2 of Array.from(document.querySelectorAll(Es)))
          Cs.sidebarInterface(t2);
      }), f(Cs);
      const Ts = "ArrowLeft", Os = "ArrowRight", ks = "ArrowUp", xs = "ArrowDown", Ls = "active", Ds = "fade", Ss = "show", Ns = '[data-coreui-toggle="tab"], [data-coreui-toggle="pill"], [data-coreui-toggle="list"]', Is = `.nav-link:not(.dropdown-toggle), .list-group-item:not(.dropdown-toggle), [role="tab"]:not(.dropdown-toggle), ${Ns}`;
      class Ms extends B {
        constructor(t2) {
          super(t2), this._parent = this._element.closest('.list-group, .nav, [role="tablist"]'), this._parent && (this._setInitialAttributes(this._parent, this._getChildren()), N.on(this._element, "keydown.coreui.tab", (t3) => this._keydown(t3)));
        }
        static get NAME() {
          return "tab";
        }
        show() {
          const t2 = this._element;
          if (this._elemIsActive(t2))
            return;
          const e2 = this._getActiveElem(), i2 = e2 ? N.trigger(e2, "hide.coreui.tab", { relatedTarget: t2 }) : null;
          N.trigger(t2, "show.coreui.tab", { relatedTarget: e2 }).defaultPrevented || i2 && i2.defaultPrevented || (this._deactivate(e2, t2), this._activate(t2, e2));
        }
        _activate(t2, e2) {
          t2 && (t2.classList.add(Ls), this._activate(z.getElementFromSelector(t2)), this._queueCallback(() => {
            "tab" === t2.getAttribute("role") ? (t2.removeAttribute("tabindex"), t2.setAttribute("aria-selected", true), this._toggleDropDown(t2, true), N.trigger(t2, "shown.coreui.tab", { relatedTarget: e2 })) : t2.classList.add(Ss);
          }, t2, t2.classList.contains(Ds)));
        }
        _deactivate(t2, e2) {
          t2 && (t2.classList.remove(Ls), t2.blur(), this._deactivate(z.getElementFromSelector(t2)), this._queueCallback(() => {
            "tab" === t2.getAttribute("role") ? (t2.setAttribute("aria-selected", false), t2.setAttribute("tabindex", "-1"), this._toggleDropDown(t2, false), N.trigger(t2, "hidden.coreui.tab", { relatedTarget: e2 })) : t2.classList.remove(Ss);
          }, t2, t2.classList.contains(Ds)));
        }
        _keydown(t2) {
          if (![Ts, Os, ks, xs].includes(t2.key))
            return;
          t2.stopPropagation(), t2.preventDefault();
          const e2 = [Os, xs].includes(t2.key), i2 = m(this._getChildren().filter((t3) => !r(t3)), t2.target, e2, true);
          i2 && (i2.focus({ preventScroll: true }), Ms.getOrCreateInstance(i2).show());
        }
        _getChildren() {
          return z.find(Is, this._parent);
        }
        _getActiveElem() {
          return this._getChildren().find((t2) => this._elemIsActive(t2)) || null;
        }
        _setInitialAttributes(t2, e2) {
          this._setAttributeIfNotExists(t2, "role", "tablist");
          for (const t3 of e2)
            this._setInitialAttributesOnChild(t3);
        }
        _setInitialAttributesOnChild(t2) {
          t2 = this._getInnerElement(t2);
          const e2 = this._elemIsActive(t2), i2 = this._getOuterElement(t2);
          t2.setAttribute("aria-selected", e2), i2 !== t2 && this._setAttributeIfNotExists(i2, "role", "presentation"), e2 || t2.setAttribute("tabindex", "-1"), this._setAttributeIfNotExists(t2, "role", "tab"), this._setInitialAttributesOnTargetPanel(t2);
        }
        _setInitialAttributesOnTargetPanel(t2) {
          const e2 = z.getElementFromSelector(t2);
          e2 && (this._setAttributeIfNotExists(e2, "role", "tabpanel"), t2.id && this._setAttributeIfNotExists(e2, "aria-labelledby", `#${t2.id}`));
        }
        _toggleDropDown(t2, e2) {
          const i2 = this._getOuterElement(t2);
          if (!i2.classList.contains("dropdown"))
            return;
          const n2 = (t3, n3) => {
            const s2 = z.findOne(t3, i2);
            s2 && s2.classList.toggle(n3, e2);
          };
          n2(".dropdown-toggle", Ls), n2(".dropdown-menu", Ss), i2.setAttribute("aria-expanded", e2);
        }
        _setAttributeIfNotExists(t2, e2, i2) {
          t2.hasAttribute(e2) || t2.setAttribute(e2, i2);
        }
        _elemIsActive(t2) {
          return t2.classList.contains(Ls);
        }
        _getInnerElement(t2) {
          return t2.matches(Is) ? t2 : z.findOne(Is, t2);
        }
        _getOuterElement(t2) {
          return t2.closest(".nav-item, .list-group-item") || t2;
        }
        static jQueryInterface(t2) {
          return this.each(function() {
            const e2 = Ms.getOrCreateInstance(this);
            if ("string" == typeof t2) {
              if (void 0 === e2[t2] || t2.startsWith("_") || "constructor" === t2)
                throw new TypeError(`No method named "${t2}"`);
              e2[t2]();
            }
          });
        }
      }
      N.on(document, "click.coreui.tab", Ns, function(t2) {
        ["A", "AREA"].includes(this.tagName) && t2.preventDefault(), r(this) || Ms.getOrCreateInstance(this).show();
      }), N.on(window, "load.coreui.tab", () => {
        for (const t2 of z.find('.active[data-coreui-toggle="tab"], .active[data-coreui-toggle="pill"], .active[data-coreui-toggle="list"]'))
          Ms.getOrCreateInstance(t2);
      }), f(Ms);
      const Ps = ".coreui.toast", js = `mouseover${Ps}`, $s = `mouseout${Ps}`, Fs = `focusin${Ps}`, Hs = `focusout${Ps}`, Bs = `hide${Ps}`, Ws = `hidden${Ps}`, zs = `show${Ps}`, qs = `shown${Ps}`, Rs = "hide", Vs = "show", Us = "showing", Ks = { animation: "boolean", autohide: "boolean", delay: "number" }, Qs = { animation: true, autohide: true, delay: 5e3 };
      class Ys extends B {
        constructor(t2, e2) {
          super(t2, e2), this._timeout = null, this._hasMouseInteraction = false, this._hasKeyboardInteraction = false, this._setListeners();
        }
        static get Default() {
          return Qs;
        }
        static get DefaultType() {
          return Ks;
        }
        static get NAME() {
          return "toast";
        }
        show() {
          N.trigger(this._element, zs).defaultPrevented || (this._clearTimeout(), this._config.animation && this._element.classList.add("fade"), this._element.classList.remove(Rs), c(this._element), this._element.classList.add(Vs, Us), this._queueCallback(() => {
            this._element.classList.remove(Us), N.trigger(this._element, qs), this._maybeScheduleHide();
          }, this._element, this._config.animation));
        }
        hide() {
          this.isShown() && (N.trigger(this._element, Bs).defaultPrevented || (this._element.classList.add(Us), this._queueCallback(() => {
            this._element.classList.add(Rs), this._element.classList.remove(Us, Vs), N.trigger(this._element, Ws);
          }, this._element, this._config.animation)));
        }
        dispose() {
          this._clearTimeout(), this.isShown() && this._element.classList.remove(Vs), super.dispose();
        }
        isShown() {
          return this._element.classList.contains(Vs);
        }
        _maybeScheduleHide() {
          this._config.autohide && (this._hasMouseInteraction || this._hasKeyboardInteraction || (this._timeout = setTimeout(() => {
            this.hide();
          }, this._config.delay)));
        }
        _onInteraction(t2, e2) {
          switch (t2.type) {
            case "mouseover":
            case "mouseout":
              this._hasMouseInteraction = e2;
              break;
            case "focusin":
            case "focusout":
              this._hasKeyboardInteraction = e2;
          }
          if (e2)
            return void this._clearTimeout();
          const i2 = t2.relatedTarget;
          this._element === i2 || this._element.contains(i2) || this._maybeScheduleHide();
        }
        _setListeners() {
          N.on(this._element, js, (t2) => this._onInteraction(t2, true)), N.on(this._element, $s, (t2) => this._onInteraction(t2, false)), N.on(this._element, Fs, (t2) => this._onInteraction(t2, true)), N.on(this._element, Hs, (t2) => this._onInteraction(t2, false));
        }
        _clearTimeout() {
          clearTimeout(this._timeout), this._timeout = null;
        }
        static jQueryInterface(t2) {
          return this.each(function() {
            const e2 = Ys.getOrCreateInstance(this, t2);
            if ("string" == typeof t2) {
              if (void 0 === e2[t2])
                throw new TypeError(`No method named "${t2}"`);
              e2[t2](this);
            }
          });
        }
      }
      return q(Ys), f(Ys), { Alert: R, Button: U, Carousel: pt, Collapse: kt, Dropdown: Li, Modal: tn, Navigation: pn, OffCanvas: Sn, Popover: ts, ScrollSpy: cs, Sidebar: Cs, Tab: Ms, Toast: Ys, Tooltip: Gn };
    });
  }
});
export default require_coreui_bundle_min();
/*!
  * CoreUI [object Object] v4.2.6 (https://coreui.io)
  * Copyright 2022 The CoreUI Team (https://github.com/orgs/coreui/people)
  * Licensed under MIT (https://coreui.io)
  */
//# sourceMappingURL=@coreui_coreui_dist_js_coreui__bundle__min.js.map
